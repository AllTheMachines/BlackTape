---
phase: 18-ai-auto-news
plan: 05
type: execute
wave: 3
depends_on:
  - 18-03
  - 18-04
files_modified:
  - src/routes/artist/[slug]/+page.svelte
  - tools/test-suite/manifest.mjs
autonomous: true
requirements:
  - NEWS-01
  - NEWS-02
  - NEWS-03

must_haves:
  truths:
    - "ArtistSummary component appears on the artist page below the header and above releases in the overview tab"
    - "The artist page passes artistMbid, artistName, artistTags, and releases props to ArtistSummary"
    - "Test manifest has Phase 18 code-check entries for ArtistSummary.svelte existence and key content"
    - "npm run check passes after all wiring"
  artifacts:
    - path: "src/routes/artist/[slug]/+page.svelte"
      provides: "ArtistSummary imported and rendered in overview tab above releases"
      contains: "ArtistSummary"
    - path: "tools/test-suite/manifest.mjs"
      provides: "PHASE_18 test array with code-check entries"
      contains: "PHASE_18"
  key_links:
    - from: "src/routes/artist/[slug]/+page.svelte"
      to: "src/lib/components/ArtistSummary.svelte"
      via: "import ArtistSummary"
      pattern: "ArtistSummary"
    - from: "tools/test-suite/manifest.mjs"
      to: "src/lib/components/ArtistSummary.svelte"
      via: "fileExists + fileContains checks"
      pattern: "P18-"
---

<objective>
Wire ArtistSummary into the artist page and add Phase 18 entries to the test suite manifest.

Purpose: ArtistSummary.svelte exists but is invisible until dropped into the artist page. This plan makes it live. The test manifest entries create a lasting automated check that Phase 18 artifacts remain intact.

Output: Artist page renders ArtistSummary; test manifest has P18 code checks.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-ai-auto-news/18-CONTEXT.md
@.planning/phases/18-ai-auto-news/18-03-SUMMARY.md
@src/routes/artist/[slug]/+page.svelte
@tools/test-suite/manifest.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import and render ArtistSummary in artist +page.svelte</name>
  <files>src/routes/artist/[slug]/+page.svelte</files>
  <action>
    Read the current `src/routes/artist/[slug]/+page.svelte` to understand its import section and template structure.

    **Script addition:**
    Add the import alongside the existing component imports at the top of the `<script>` block:
    ```typescript
    import ArtistSummary from '$lib/components/ArtistSummary.svelte';
    ```

    **Template addition:**
    Find the overview tab content (the `{#if activeTab === 'overview'}` block or equivalent). Locate the position BELOW the artist header section (bio, tags, header meta) and ABOVE the releases grid / release cards section.

    Insert the component at that position:
    ```svelte
    <ArtistSummary
      artistMbid={data.artist.mbid}
      artistName={data.artist.name}
      artistTags={data.artist.tags ?? ''}
      releases={data.releases}
    />
    ```

    **Prop mapping:**
    - `artistMbid` ← `data.artist.mbid` (the MusicBrainz ID, used as cache key)
    - `artistName` ← `data.artist.name`
    - `artistTags` ← `data.artist.tags ?? ''` (tags may be null if artist has none)
    - `releases` ← `data.releases` (array already loaded by +page.ts, contains title/year/type)

    The summary must appear in the **overview tab only** — it should be within the `{#if activeTab === 'overview'}` block (or equivalent conditional). If the artist page uses a flat layout without tabs, place it below bio and above the releases section.

    Do NOT place it in the stats tab. Do NOT change how the existing `aiBio` fallback works — leave that code unchanged.
  </action>
  <verify>
    <automated>cd D:/Projects/Mercury && npm run check 2>&1</automated>
  </verify>
  <done>npm run check passes; ArtistSummary imported in +page.svelte; component rendered in overview tab with all four props (artistMbid, artistName, artistTags, releases)</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 18 entries to test suite manifest</name>
  <files>tools/test-suite/manifest.mjs</files>
  <action>
    Read the current `tools/test-suite/manifest.mjs` to understand the pattern for code-check tests and the export structure at the bottom.

    Add a new `PHASE_18` array after the existing phase arrays (e.g., after PHASE_17):

    ```javascript
    // ---------------------------------------------------------------------------
    // PHASE 18 — AI Auto-News
    // ---------------------------------------------------------------------------

    export const PHASE_18 = [
      {
        id: 'P18-01', phase: 18, area: 'AI Summary',
        desc: 'ArtistSummary.svelte component exists',
        method: 'code',
        fn: () => fileExists('src/lib/components/ArtistSummary.svelte'),
      },
      {
        id: 'P18-02', phase: 18, area: 'AI Summary',
        desc: 'ArtistSummary.svelte contains data-testid="ai-summary"',
        method: 'code',
        fn: () => fileContains('src/lib/components/ArtistSummary.svelte', 'data-testid="ai-summary"'),
      },
      {
        id: 'P18-03', phase: 18, area: 'AI Summary',
        desc: 'ArtistSummary.svelte contains attribution label text',
        method: 'code',
        fn: () => fileContains('src/lib/components/ArtistSummary.svelte', 'AI summary based on MusicBrainz data'),
      },
      {
        id: 'P18-04', phase: 18, area: 'AI Summary',
        desc: 'ArtistSummary.svelte uses artistSummaryFromReleases (not old PROMPTS.artistSummary)',
        method: 'code',
        fn: () => fileContains('src/lib/components/ArtistSummary.svelte', 'artistSummaryFromReleases'),
      },
      {
        id: 'P18-05', phase: 18, area: 'AI Summary',
        desc: 'artist_summaries table DDL exists in taste_db.rs',
        method: 'code',
        fn: () => fileContains('src-tauri/src/ai/taste_db.rs', 'artist_summaries'),
      },
      {
        id: 'P18-06', phase: 18, area: 'AI Summary',
        desc: 'get_artist_summary command registered in lib.rs',
        method: 'code',
        fn: () => fileContains('src-tauri/src/lib.rs', 'get_artist_summary'),
      },
      {
        id: 'P18-07', phase: 18, area: 'AI Settings',
        desc: 'AI_PROVIDERS constant exists in providers.ts',
        method: 'code',
        fn: () => fileContains('src/lib/ai/providers.ts', 'AI_PROVIDERS'),
      },
      {
        id: 'P18-08', phase: 18, area: 'AI Settings',
        desc: 'AiSettings.svelte imports AI_PROVIDERS',
        method: 'code',
        fn: () => fileContains('src/lib/components/AiSettings.svelte', 'AI_PROVIDERS'),
      },
      {
        id: 'P18-09', phase: 18, area: 'AI Settings',
        desc: 'AiSettings.svelte has affiliate badge text',
        method: 'code',
        fn: () => fileContains('src/lib/components/AiSettings.svelte', 'affiliate link'),
      },
      {
        id: 'P18-10', phase: 18, area: 'AI Settings',
        desc: 'artistSummaryFromReleases exported from prompts.ts',
        method: 'code',
        fn: () => fileContains('src/lib/ai/prompts.ts', 'artistSummaryFromReleases'),
      },
      {
        id: 'P18-11', phase: 18, area: 'AI Summary',
        desc: 'Artist page +page.svelte includes ArtistSummary component',
        method: 'code',
        fn: () => fileContains('src/routes/artist/[slug]/+page.svelte', 'ArtistSummary'),
      },
      {
        id: 'P18-12', phase: 18, area: 'AI Summary',
        desc: 'ArtistSummary renders on live artist page with summary visible',
        method: 'tauri',
        fn: async (page) => {
          await page.goto('tauri://localhost/artist/radiohead');
          await page.waitForTimeout(500);
          // Summary section only visible if cache exists — skip if not present
          const summaryEl = page.locator('[data-testid="ai-summary"]');
          const count = await summaryEl.count();
          // Pass if summary is visible OR if section is correctly hidden (no cache)
          return count === 0 || await summaryEl.isVisible();
        },
      },
    ];
    ```

    Then find the `export` statement at the bottom of manifest.mjs where all phases are combined (something like `export const ALL_TESTS = [...PHASE_2, ...PHASE_3, ...]`) and add `...PHASE_18` to that array.

    Also find where PHASE_17 or the last phase was added and ensure PHASE_18 follows the same pattern.
  </action>
  <verify>
    <automated>cd D:/Projects/Mercury && node tools/test-suite/run.mjs --code-only --phase 18 2>&1</automated>
  </verify>
  <done>Test runner executes Phase 18 code checks without crashing; all P18-01 through P18-11 code checks pass; P18-12 is tauri method so skipped in code-only mode</done>
</task>

</tasks>

<verification>
Run `npm run check` — must pass.
Run `node tools/test-suite/run.mjs --code-only --phase 18` — all code-check tests must pass.
Grep `ArtistSummary` in `src/routes/artist/[slug]/+page.svelte` — import and usage found.
Grep `P18-` in `tools/test-suite/manifest.mjs` — at least 12 entries found.
</verification>

<success_criteria>
- `npm run check` passes
- `node tools/test-suite/run.mjs --code-only --phase 18` runs and all code checks pass (P18-01 through P18-11)
- ArtistSummary appears in artist page overview tab with all four correct props
- PHASE_18 array added to manifest and included in ALL_TESTS export
</success_criteria>

<output>
After completion, create `.planning/phases/18-ai-auto-news/18-05-SUMMARY.md`
</output>
