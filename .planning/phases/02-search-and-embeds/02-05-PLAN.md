---
phase: 02-search-and-embeds
plan: 05
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can visit the landing page, search for an artist, click a result, and hear music"
    - "The full discovery loop works: search → results → artist page → play"
    - "Dark aesthetic is consistent across all pages"
    - "Mobile experience is usable"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 2 experience end-to-end.

Purpose: Before marking Phase 2 complete, verify the full discovery loop works: land on Mercury, search, find an artist, reach their page, and press play. This checkpoint catches integration issues that individual plan verifications miss.

Output: User confirmation that the core experience works, or a list of issues to address.
</objective>

<execution_context>
@C:\Users\User\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\User\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-search-and-embeds/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks</name>
  <files></files>
  <action>
    1. Run `npm run check` — TypeScript + Svelte checks must pass with zero errors
    2. Run `npm run build` — production build must succeed
    3. Start dev server with `npm run dev` and verify these routes load without errors:
       - `/` (landing page)
       - `/search?q=radiohead&mode=artist` (artist search)
       - `/search?q=electronic&mode=tag` (tag search)
       - `/artist/radiohead` (artist page — slug may differ, use whatever slug exists in DB)
    4. Check console for any runtime errors or warnings
    5. Test responsive layout by checking that CSS grid breakpoints are defined correctly
    6. Report findings for the human verification step
  </action>
  <verify>All four routes load. `npm run check` and `npm run build` pass. No console errors.</verify>
  <done>Automated checks complete. Ready for human visual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 2: Mercury search engine with artist pages and embedded players. The full discovery loop from search to play.</what-built>
  <how-to-verify>
    1. Open http://localhost:5173 (or whatever port dev server uses)
    2. **Landing page:** Verify dark theme, centered search bar, project name/tagline, mode toggle (Artists/Tags)
    3. **Artist search:** Type "radiohead" and press Enter. Verify:
       - Card grid of results (not a list)
       - Each card shows: artist name, country, tags (as chips), match reason
       - Clicking a tag chip navigates to tag search
    4. **Tag search:** Switch to Tags mode, type "electronic", press Enter. Verify:
       - Results show artists tagged "electronic"
       - Match reason shows the tag and count
    5. **Artist page:** Click an artist name from results. Verify:
       - Clean URL (e.g., /artist/radiohead)
       - Two-column layout: info (left) and embeds (right)
       - Tags displayed as clickable chips
       - Country and artist type shown
       - External links or embedded players present (Bandcamp as link, Spotify/SoundCloud/YouTube as embeds when available)
       - Bio snippet if available
    6. **Mobile:** Resize browser to mobile width. Verify single-column layout, usable search bar
    7. **Aesthetic:** Overall feel is dark, minimal, information-dense, utilitarian. No flashy animations. Clean.
  </how-to-verify>
  <resume-signal>Type "approved" if the experience works, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Full discovery loop works: landing → search → results → artist page → listen
2. All pages render correctly with dark theme
3. No TypeScript or build errors
4. Mobile responsive layout
5. User confirms visual quality matches expectations
</verification>

<success_criteria>
- User has verified the complete Phase 2 experience
- No blocking issues remain
- The "holy shit" moment is real: searching 2.8M artists instantly, finding unknowns, pressing play
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-and-embeds/02-05-SUMMARY.md`
</output>
