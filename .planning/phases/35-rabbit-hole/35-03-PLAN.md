---
phase: 35-rabbit-hole
plan: "03"
type: execute
wave: 2
depends_on:
  - "35-01"
  - "35-02"
files_modified:
  - src/routes/rabbit-hole/+page.svelte
  - src/routes/rabbit-hole/+page.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Landing page shows a search input that queries both artists and tags as the user types"
    - "Search results show two groups: Artists and Genres/Tags"
    - "Clicking an artist result navigates to /rabbit-hole/artist/[slug] and pushes to trail"
    - "Clicking a tag result navigates to /rabbit-hole/tag/[slug] and pushes to trail"
    - "Random button navigates immediately to a random artist card"
    - "Landing page shows when navigating to /rabbit-hole with no slug"
  artifacts:
    - path: "src/routes/rabbit-hole/+page.svelte"
      provides: "Landing page: search input + autocomplete dropdown + Random button"
      min_lines: 80
    - path: "src/routes/rabbit-hole/+page.ts"
      provides: "Minimal load function (no server data needed for landing)"
  key_links:
    - from: "src/routes/rabbit-hole/+page.svelte"
      to: "src/lib/db/queries.ts"
      via: "searchArtistsAutocomplete + searchTagsAutocomplete on input"
      pattern: "searchArtistsAutocomplete|searchTagsAutocomplete"
    - from: "src/routes/rabbit-hole/+page.svelte"
      to: "src/lib/rabbit-hole/trail.svelte.ts"
      via: "pushTrailItem on selection"
      pattern: "pushTrailItem"
    - from: "Random button"
      to: "getRandomArtist"
      via: "onClick → getRandomArtist() → goto('/rabbit-hole/artist/[slug]')"
      pattern: "getRandomArtist"
---

<objective>
Build the Rabbit Hole landing page: a unified search input (artists + tags) with grouped autocomplete dropdown and a Random button that instantly navigates to a random artist card.

Purpose: Entry point into the Rabbit Hole. User either searches for a specific artist/genre or hits Random to start exploring.
Output: `+page.svelte` (search UI) and `+page.ts` (minimal load).
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-rabbit-hole/35-CONTEXT.md
@.planning/phases/35-rabbit-hole/35-RESEARCH.md

@src/lib/db/queries.ts
@src/lib/rabbit-hole/trail.svelte.ts
@src/routes/rabbit-hole/+layout.svelte
</context>

<interfaces>
<!-- Query functions from plan 35-01 -->

From src/lib/db/queries.ts (added in plan 35-01):
```typescript
export async function searchArtistsAutocomplete(
  db: DbProvider,
  query: string,
  limit = 5
): Promise<Array<{ name: string; slug: string; tags: string | null }>>

export async function searchTagsAutocomplete(
  db: DbProvider,
  query: string,
  limit = 8
): Promise<Array<{ tag: string; artist_count: number }>>

export async function getRandomArtist(db: DbProvider): Promise<ArtistResult | null>
```

From src/lib/rabbit-hole/trail.svelte.ts (added in plan 35-01):
```typescript
export type TrailItem =
  | { type: 'artist'; slug: string; name: string }
  | { type: 'tag'; slug: string; name: string };
export function pushTrailItem(item: TrailItem): void
```

DB provider pattern used in other +page.ts files:
```typescript
import type { PageLoad } from './$types';
export const load: PageLoad = async () => {
  // landing needs no server data
  return {};
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Landing page load function (+page.ts)</name>
  <files>src/routes/rabbit-hole/+page.ts</files>
  <action>
    Create a minimal `+page.ts` for the landing page. The landing needs no server data — search happens client-side on input events.

    ```typescript
    import type { PageLoad } from './$types';

    export const load: PageLoad = async () => {
      return {};
    };
    ```

    This follows the pattern of other lightweight page.ts files in the project. All DB access happens in the component's reactive statements.
  </action>
  <verify>
    <automated>cd D:/Projects/BlackTape && npm run check 2>&1 | tail -10</automated>
  </verify>
  <done>
    - File exists at `src/routes/rabbit-hole/+page.ts`
    - `npm run check` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Landing page component (+page.svelte)</name>
  <files>src/routes/rabbit-hole/+page.svelte</files>
  <action>
    Create the landing page `+page.svelte`. This is the entry view users see when navigating to `/rabbit-hole`.

    **Features:**
    - Large centered layout: site/section title, search input, Random button
    - Search input: debounced (200ms), queries both `searchArtistsAutocomplete` and `searchTagsAutocomplete` in parallel
    - Results grouped: "Artists" section first, then "Genres & Tags" section
    - Keyboard: arrow keys navigate results, Enter selects, Escape clears
    - Clicking artist → `pushTrailItem({ type: 'artist', ... })` then `goto('/rabbit-hole/artist/[slug]', { keepFocus: true, noScroll: true })`
    - Clicking tag → `pushTrailItem({ type: 'tag', ... })` then `goto('/rabbit-hole/tag/[slug]', { keepFocus: true, noScroll: true })`
    - Random button: calls `getRandomArtist()`, if result found → pushes to trail and navigates; if null → shows brief "No artists found" (edge case)
    - Loading state on Random button while fetching

    ```svelte
    <script lang="ts">
      import { goto } from '$app/navigation';
      import { onMount } from 'svelte';
      import { getProvider } from '$lib/db/provider';
      import { searchArtistsAutocomplete, searchTagsAutocomplete, getRandomArtist } from '$lib/db/queries';
      import { pushTrailItem } from '$lib/rabbit-hole/trail.svelte';
      import type { DbProvider } from '$lib/db/provider';

      let db = $state<DbProvider | null>(null);
      let query = $state('');
      let artistResults = $state<Array<{ name: string; slug: string; tags: string | null }>>([]);
      let tagResults = $state<Array<{ tag: string; artist_count: number }>>([]);
      let searching = $state(false);
      let randomLoading = $state(false);
      let debounceTimer = $state<ReturnType<typeof setTimeout> | null>(null);
      let hasResults = $derived(artistResults.length > 0 || tagResults.length > 0);

      onMount(async () => {
        db = await getProvider();
      });

      async function handleInput() {
        if (debounceTimer) clearTimeout(debounceTimer);
        const q = query.trim();
        if (!q) {
          artistResults = [];
          tagResults = [];
          return;
        }
        debounceTimer = setTimeout(async () => {
          if (!db) return;
          searching = true;
          try {
            const [artists, tags] = await Promise.all([
              searchArtistsAutocomplete(db, q, 6),
              searchTagsAutocomplete(db, q, 6)
            ]);
            artistResults = artists;
            tagResults = tags;
          } catch {
            artistResults = [];
            tagResults = [];
          } finally {
            searching = false;
          }
        }, 200);
      }

      function selectArtist(name: string, slug: string) {
        pushTrailItem({ type: 'artist', slug, name });
        query = '';
        artistResults = [];
        tagResults = [];
        goto(`/rabbit-hole/artist/${slug}`, { keepFocus: true, noScroll: true });
      }

      function selectTag(tag: string) {
        pushTrailItem({ type: 'tag', slug: encodeURIComponent(tag), name: tag });
        query = '';
        artistResults = [];
        tagResults = [];
        goto(`/rabbit-hole/tag/${encodeURIComponent(tag)}`, { keepFocus: true, noScroll: true });
      }

      async function handleRandom() {
        if (!db || randomLoading) return;
        randomLoading = true;
        try {
          const artist = await getRandomArtist(db);
          if (artist) {
            pushTrailItem({ type: 'artist', slug: artist.slug, name: artist.name });
            goto(`/rabbit-hole/artist/${artist.slug}`, { keepFocus: true, noScroll: true });
          }
        } catch {
          // silently fail
        } finally {
          randomLoading = false;
        }
      }
    </script>

    <div class="rh-landing">
      <div class="rh-landing-inner">
        <p class="rh-tagline">Where do you want to go?</p>

        <div class="rh-search-wrap">
          <input
            class="rh-search-input"
            type="text"
            placeholder="Search artists, genres, tags..."
            bind:value={query}
            oninput={handleInput}
            autocomplete="off"
            spellcheck="false"
          />
          {#if hasResults || searching}
            <div class="rh-results" role="listbox">
              {#if searching}
                <div class="rh-results-loading">Searching...</div>
              {:else}
                {#if artistResults.length > 0}
                  <div class="rh-results-group">
                    <div class="rh-results-label">Artists</div>
                    {#each artistResults as a}
                      <button
                        class="rh-result-item"
                        role="option"
                        aria-selected="false"
                        onclick={() => selectArtist(a.name, a.slug)}
                      >
                        <span class="rh-result-name">{a.name}</span>
                        {#if a.tags}
                          <span class="rh-result-meta">{a.tags.split(',')[0].trim()}</span>
                        {/if}
                      </button>
                    {/each}
                  </div>
                {/if}
                {#if tagResults.length > 0}
                  <div class="rh-results-group">
                    <div class="rh-results-label">Genres & Tags</div>
                    {#each tagResults as t}
                      <button
                        class="rh-result-item"
                        role="option"
                        aria-selected="false"
                        onclick={() => selectTag(t.tag)}
                      >
                        <span class="rh-result-name">◈ {t.tag}</span>
                        <span class="rh-result-meta">{t.artist_count.toLocaleString()} artists</span>
                      </button>
                    {/each}
                  </div>
                {/if}
              {/if}
            </div>
          {/if}
        </div>

        <button class="rh-random-btn" onclick={handleRandom} disabled={randomLoading}>
          {randomLoading ? 'Finding...' : 'Random'}
        </button>
      </div>
    </div>

    <style>
      .rh-landing {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100%;
        padding: var(--space-xl);
      }

      .rh-landing-inner {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: var(--space-lg);
        width: 100%;
        max-width: 520px;
      }

      .rh-tagline {
        color: var(--t-3);
        font-size: 0.875rem;
        margin: 0;
      }

      .rh-search-wrap {
        position: relative;
        width: 100%;
      }

      .rh-search-input {
        width: 100%;
        padding: 12px 16px;
        background: var(--bg-2);
        border: 1px solid var(--b-2);
        border-radius: var(--radius-md);
        color: var(--t-1);
        font-size: 1rem;
        outline: none;
        transition: border-color 0.15s;
        box-sizing: border-box;
      }

      .rh-search-input:focus {
        border-color: var(--acc);
      }

      .rh-results {
        position: absolute;
        top: calc(100% + 4px);
        left: 0;
        right: 0;
        background: var(--bg-2);
        border: 1px solid var(--b-2);
        border-radius: var(--radius-md);
        z-index: 50;
        overflow: hidden;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
      }

      .rh-results-loading {
        padding: 12px 16px;
        color: var(--t-3);
        font-size: 0.875rem;
      }

      .rh-results-group {
        padding: 6px 0;
      }

      .rh-results-label {
        padding: 4px 16px;
        font-size: 0.6875rem;
        font-weight: 600;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--t-4);
      }

      .rh-result-item {
        display: flex;
        align-items: center;
        justify-content: space-between;
        width: 100%;
        padding: 8px 16px;
        background: none;
        border: none;
        color: var(--t-1);
        cursor: pointer;
        text-align: left;
        font-size: 0.875rem;
        gap: var(--space-sm);
        transition: background 0.1s;
      }

      .rh-result-item:hover {
        background: var(--bg-3);
      }

      .rh-result-name {
        flex: 1;
        min-width: 0;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
      }

      .rh-result-meta {
        color: var(--t-4);
        font-size: 0.75rem;
        flex-shrink: 0;
      }

      .rh-random-btn {
        padding: 10px 32px;
        background: var(--bg-3);
        border: 1px solid var(--b-2);
        border-radius: var(--radius-md);
        color: var(--t-1);
        font-size: 0.9375rem;
        cursor: pointer;
        transition: background 0.15s, border-color 0.15s;
      }

      .rh-random-btn:hover:not(:disabled) {
        background: var(--bg-4, var(--bg-3));
        border-color: var(--acc);
      }

      .rh-random-btn:disabled {
        opacity: 0.5;
        cursor: not-allowed;
      }
    </style>
    ```

    CSS variables: use only design system variables (`--bg-2`, `--bg-3`, `--b-2`, `--t-1`, `--t-3`, `--t-4`, `--acc`, `--radius-md`, `--space-sm`, `--space-lg`, `--space-xl`). These are all defined in the project theme. `--bg-4` has a fallback to `--bg-3` in case it's not defined.
  </action>
  <verify>
    <automated>cd D:/Projects/BlackTape && npm run check 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npm run check` passes
    - `src/routes/rabbit-hole/+page.svelte` exists
    - Landing shows search input + Random button
    - Searching artists and tags runs in parallel on input
    - Results grouped into Artists / Genres & Tags
    - Selection pushes to trail and navigates in-place
    - Random button calls `getRandomArtist()` and navigates
  </done>
</task>

</tasks>

<verification>
`npm run check` passes. Navigate to `/rabbit-hole` in app — centered search input and Random button visible. Typing a query shows grouped autocomplete. Clicking a result navigates to the appropriate Rabbit Hole page. Trail bar shows visited items.
</verification>

<success_criteria>
- `npm run check` passes with zero errors
- `src/routes/rabbit-hole/+page.svelte` renders search + Random button
- Search queries both artists and tags in parallel with 200ms debounce
- Artist results navigate to `/rabbit-hole/artist/[slug]`; tag results to `/rabbit-hole/tag/[slug]`
- Random navigates to a random artist card
- Trail is updated on each navigation
</success_criteria>

<output>
After completion, create `.planning/phases/35-rabbit-hole/35-03-SUMMARY.md`
</output>
