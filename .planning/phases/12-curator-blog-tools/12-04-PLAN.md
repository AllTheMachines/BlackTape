---
phase: 12-curator-blog-tools
plan: 04
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
  - 12-03
files_modified:
  - src/routes/new-rising/+page.svelte
  - src/routes/new-rising/+page.server.ts
  - src/routes/api/new-rising/+server.ts
  - src/routes/api/rss/new-rising/+server.ts
  - src/routes/+layout.svelte
  - ARCHITECTURE.md
  - docs/user-manual.md
autonomous: true
requirements:
  - BLOG-01
  - BLOG-02
  - BLOG-03

must_haves:
  truths:
    - "Visiting /new-rising shows two views: 'New Artists' (recently active) and 'Gaining Traction' (niche tags + recent)"
    - "New & Rising page is public — no login required, available on both web and Tauri"
    - "Each view shows artist cards with name, tags, country, and link to full artist page"
    - "30-day window (begin_year >= current year - 1 proxy) defines the 'recently active' filter"
    - "New & Rising page has an RSS feed link (/api/rss/new-rising)"
    - "GET /api/rss/new-rising returns RSS/Atom feed of the gaining-traction artists"
    - "ARCHITECTURE.md has a Curator / Blog Tools section documenting the new surfaces"
    - "docs/user-manual.md has a Curator Tools section documenting embed widgets, RSS, attribution, and New & Rising"
    - "npm run check passes with zero TypeScript errors"
  artifacts:
    - path: "src/routes/new-rising/+page.svelte"
      provides: "New & Rising public discovery page with two-tab view"
      min_lines: 50
    - path: "src/routes/new-rising/+page.server.ts"
      provides: "Server load for New & Rising page — queries D1"
      exports: ["load"]
    - path: "src/routes/api/new-rising/+server.ts"
      provides: "JSON API for New & Rising data (used by Tauri universal load)"
      exports: ["GET"]
    - path: "src/routes/api/rss/new-rising/+server.ts"
      provides: "RSS/Atom feed for gaining-traction artists — queries D1 artist_tags + tag_stats"
      exports: ["GET"]
    - path: "ARCHITECTURE.md"
      provides: "Curator / Blog Tools section"
      contains: "Curator / Blog Tools"
    - path: "docs/user-manual.md"
      provides: "Curator Tools section"
      contains: "Curator Tools"
  key_links:
    - from: "src/routes/new-rising/+page.server.ts"
      to: "D1 artists + artist_tags + tag_stats tables"
      via: "SQL query for recently-active niche artists"
      pattern: "begin_year|tag_stats"
    - from: "src/routes/api/new-rising/+server.ts"
      to: "src/routes/new-rising/+page.server.ts"
      via: "Same query logic — shared or duplicated for universal load pattern"
      pattern: "new-rising"
    - from: "src/routes/api/rss/new-rising/+server.ts"
      to: "D1 artist_tags + tag_stats tables"
      via: "gaining-traction SQL query (same as /api/new-rising gainingTraction query: begin_year filter + AVG tag rarity order)"
      pattern: "tag_stats|begin_year"
---

<objective>
Build the New & Rising public discovery page (two views: new artists + gaining traction), add the New & Rising RSS feed, add RSS feed buttons to applicable pages (tag and curator pages), and write the ARCHITECTURE.md + user-manual.md documentation for the entire Phase 12 curator tools system.

Purpose: This plan closes Phase 12 by completing the public-facing discovery surface for curators (BLOG-01 success criterion: "A music blogger has reason to write again") and documenting everything added this phase.

Output: /new-rising route, /api/new-rising endpoint, /api/rss/new-rising feed, nav link, and docs.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-curator-blog-tools/12-CONTEXT.md
@.planning/phases/12-curator-blog-tools/12-RESEARCH.md
@src/routes/scenes/+page.svelte
@src/routes/api/scenes/+server.ts
@ARCHITECTURE.md
@docs/user-manual.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: New & Rising page + API endpoint + RSS feed</name>
  <files>
    src/routes/new-rising/+page.server.ts
    src/routes/new-rising/+page.svelte
    src/routes/api/new-rising/+server.ts
    src/routes/api/rss/new-rising/+server.ts
    src/routes/+layout.svelte
  </files>
  <action>
    **src/routes/api/new-rising/+server.ts**
    Two queries, same pattern as `/api/scenes/+server.ts`:
    - Guard: if no `platform?.env?.DB`, return `json({ newArtists: [], gainingTraction: [] })`
    - `currentYear = new Date().getFullYear()`
    - **New Artists query** (proxy: recently active, begin_year >= currentYear - 1, not ended):
      ```sql
      SELECT a.id, a.mbid, a.name, a.slug, a.country, a.begin_year,
             (SELECT GROUP_CONCAT(tag, ', ') FROM artist_tags WHERE artist_id = a.id LIMIT 5) AS tags
      FROM artists a
      WHERE a.begin_year >= ? AND a.ended = 0
        AND a.id IN (SELECT DISTINCT artist_id FROM artist_tags)
      ORDER BY a.begin_year DESC, a.id DESC
      LIMIT 30
      ```
    - **Gaining Traction query** (niche uniqueness signal — high avg tag rarity + recent):
      ```sql
      SELECT a.id, a.mbid, a.name, a.slug, a.country, a.begin_year,
             (SELECT GROUP_CONCAT(tag, ', ') FROM artist_tags WHERE artist_id = a.id LIMIT 5) AS tags
      FROM artists a
      WHERE a.begin_year >= ? AND a.ended = 0
        AND a.id IN (SELECT DISTINCT artist_id FROM artist_tags)
      ORDER BY (
        SELECT AVG(1.0 / NULLIF(ts.artist_count, 0))
        FROM artist_tags at2
        JOIN tag_stats ts ON ts.tag = at2.tag
        WHERE at2.artist_id = a.id
      ) DESC
      LIMIT 30
      ```
      Pass `currentYear - 1` as param to both queries.
    - Wrap both in try/catch — return partial results if one fails
    - Return `json({ newArtists, gainingTraction })`

    - **Curator filter support**: if `url.searchParams.get('curator')` is set, filter `gainingTraction` (and add a curator-specific query):
      ```sql
      SELECT a.id, a.mbid, a.name, a.slug, a.country, a.begin_year,
             (SELECT GROUP_CONCAT(tag, ', ') FROM artist_tags WHERE artist_id = a.id LIMIT 5) AS tags
      FROM artists a
      JOIN curator_features cf ON cf.artist_mbid = a.mbid
      WHERE cf.curator_handle = ?
      ORDER BY cf.featured_at DESC
      LIMIT 30
      ```
      Wrap in try/catch in case curator_features doesn't exist. Return as a third key `curatorArtists`.

    **src/routes/new-rising/+page.server.ts**
    Universal load pattern (same as scenes, discover pages):
    - If `platform?.env?.DB`: query D1 directly (same queries as /api/new-rising)
    - Otherwise (Tauri): return empty arrays — Tauri fetches from /api/new-rising in +page.ts universal load

    Actually: use `+page.server.ts` for web (D1 query), and a companion `+page.ts` for universal load that calls `/api/new-rising` on Tauri (if this pattern is established for scenes). Check the scenes route — if it uses +page.server.ts only (web-first), do the same here.

    For simplicity, use +page.server.ts only (web-first, matches scenes pattern). On Tauri, the page can show a message "Visit mercury.example/new-rising for the full new & rising feed" or just show empty with a note.

    Return: `{ newArtists, gainingTraction, curatorArtists, curatorHandle, currentYear }`

    **src/routes/new-rising/+page.svelte**
    - Props: `let { data } = $props();`
    - Two-tab state: `let activeView = $state<'new' | 'traction' | 'curator'>('new')`
    - If `data.curatorHandle` is set (URL param), default to 'curator' view and show curator's artists
    - Page title: "New & Rising — Mercury"
    - Header: "New & Rising" h1 with subtitle "Artists worth hearing about right now"
    - Tab row: "Newly Active" | "Gaining Traction" (and "By @[curator]" tab if curatorHandle present)
    - Each tab shows a grid of artist cards. Each card:
      - Artist name as link to `/artist/[slug]`
      - Tags (first 4, comma-separated)
      - Country if available
      - begin_year if available
    - Empty state: "No artists found — check back as the index grows."
    - RSS link using RssButton: `<RssButton href="/api/rss/new-rising" label="New & Rising RSS feed" />` near the page header
    - CSS: use existing CSS custom properties (--bg-surface, --text-primary, etc.) — same patterns as scenes page. Grid layout: 2-3 columns on desktop, 1 on mobile.
    - Note on proxy: add a small footnote "Artists active since [currentYear-1], ordered by tag niche score." — honest about the approximation documented in RESEARCH.md.

    **src/routes/api/rss/new-rising/+server.ts**
    (Discretion item from CONTEXT.md: Claude decides whether New & Rising gets RSS — YES it should, it's the most useful feed for a blogger.)
    - Same RSS pattern as /api/rss/tag/[tag]/+server.ts
    - Query gaining-traction artists (same query as /api/new-rising)
    - Feed: `title: "New & Rising — Mercury"`, `description: "Niche artists gaining traction right now"`
    - Each artist is a feed item
    - Support ?format=atom

    **src/routes/+layout.svelte — Add New & Rising nav link**
    Find the web nav section (non-Tauri nav links). Add "New & Rising" link alongside Discover, Style Map. This is a web-first feature — no Tauri gating needed. Follow the same pattern as existing nav links.
  </action>
  <verify>
    `npm run check` passes with zero errors.
    `npm run build` succeeds.
    Visit /new-rising — two tabs render with artist cards.
    Visit /new-rising?curator=testblogger — curator tab appears (or graceful empty state).
    GET /api/new-rising returns JSON with newArtists and gainingTraction arrays.
    GET /api/rss/new-rising returns valid RSS XML.
    New & Rising link appears in web nav.
  </verify>
  <done>
    /new-rising shows two views with artist data from D1. Curator filter via URL param works. RSS feed at /api/rss/new-rising returns valid XML. Nav link added. npm run check + build pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 12 in ARCHITECTURE.md and docs/user-manual.md</name>
  <files>
    ARCHITECTURE.md
    docs/user-manual.md
  </files>
  <action>
    **ARCHITECTURE.md — Add "Curator / Blog Tools" section**
    Read ARCHITECTURE.md to find the right insertion point (after Scene Building section, following the natural phase ordering). Add a section covering:

    ```markdown
    ## Curator / Blog Tools

    Phase 12 adds surfaces for music bloggers and curators — embeddable widgets, RSS feeds, attribution tracking, and a public New & Rising discovery page.

    ### Embed Widgets

    Dedicated routes at `/embed/artist/[slug]` and `/embed/collection/[id]` serve minimal artist/collection cards with their own layout (`/embed/+layout.svelte` — replaces root layout, no Mercury nav/player/chat). Dark/light mode auto-detected via `window.matchMedia('(prefers-color-scheme: dark)')` inside the iframe context.

    Embed snippet generation: `src/lib/curator/embed-snippet.ts` — `generateEmbedSnippets(embedUrl, title, curatorHandle?)` returns both an `<iframe>` snippet and a `<script>` + `<div data-src>` snippet. The artist page (`/artist/[slug]`) shows a copy-paste embed UI with format toggle and QR code generation.

    QR code generation: `src/lib/curator/qr.ts` — `generateQrSvg(url, dark?)` wraps the `qrcode` npm package, returns SVG string for inline rendering via `{@html}`. Client-side only (dynamic import).

    ### RSS/Atom Feeds

    All four feed types served as `+server.ts` routes returning `application/rss+xml` or `application/atom+xml`. Format negotiated by `?format=atom` URL param or `Accept` header. Uses the `feed` npm package (not hand-rolled XML — handles character escaping for artist names with `&`, `<`, quotes).

    | Feed | Route | Items |
    |------|-------|-------|
    | Artist | `/api/rss/artist/[slug]` | Single item: current artist profile state |
    | Tag | `/api/rss/tag/[tag]` | Artists tagged with this tag (top 50 by count) |
    | Collection | `/api/rss/collection/[id]` | Empty on web (collections are Tauri-only) |
    | Curator | `/api/rss/curator/[handle]` | Artists featured by this curator |
    | New & Rising | `/api/rss/new-rising` | Gaining-traction niche artists |

    Artist and tag RSS feeds are informational snapshots (not event-driven) — artist profiles don't have change history. The value is "subscribe to this niche tag" for discovery.

    RssButton component: `src/lib/components/RssButton.svelte` — renders the standard RSS orange icon as an anchor. Placed on artist pages, discover page, and New & Rising page.

    ### Curator Attribution

    D1 table: `curator_features (id, artist_mbid, curator_handle, featured_at, source)` with UNIQUE(artist_mbid, curator_handle) to deduplicate.

    Attribution trigger: when a blogger includes `data-curator="[handle]"` in the script-tag embed snippet, `embed.js` fires a GET to `/api/curator-feature?artist=[mbid]&curator=[handle]`. Input validation: handle must match `/^[\w\-\.]{1,50}$/`, MBID must match UUID format. Returns 200 regardless of outcome (fire-and-forget).

    Display: artist page `+page.server.ts` queries curator_features (wrapped in try/catch — table may not exist on older DB versions). Renders as "Discovered by @handle" list linking to `/new-rising?curator=[handle]`.

    ### New & Rising Page

    Public page at `/new-rising` — no auth required, web and Tauri.

    Two views:
    - **Newly Active**: `WHERE begin_year >= (currentYear - 1) AND ended = 0` — proxy for "recently active" (no `added_at` column in artists table). Ordered by begin_year DESC.
    - **Gaining Traction**: same recency filter, ordered by average tag rarity (`AVG(1.0 / NULLIF(tag_stats.artist_count, 0))`) — surfaces niche artists accumulating unique tag combinations.

    Curator filter: `/new-rising?curator=[handle]` shows a third tab with artists featured by that curator (from curator_features table).

    ### Anti-Patterns

    | Anti-Pattern | Why Wrong | Correct Approach |
    |-------------|-----------|-----------------|
    | Hand-rolled RSS XML | Artist names with `&`, `<`, `'` break feed parsers | Always use `feed` npm package |
    | Root layout in /embed/* routes | Mercury nav/player/chat break the iframe card | /embed/+layout.svelte replaces root layout |
    | `prefers-color-scheme` via postMessage | Unnecessary — iframe reads OS preference directly | `window.matchMedia` inside the embed page |
    | `prerender = true` on RSS routes | Stale feeds baked at build time | Dynamic routes only — D1 queried on each request |
    | Blocking artist page on curator attribution | Attribution load failure breaks core page | try/catch with empty array fallback |
    ```

    **docs/user-manual.md — Add "Curator Tools" section**
    Read user-manual.md to find where to add (after Scenes section, following phase order). Add a section covering:

    ```markdown
    ## Curator Tools

    Mercury gives music bloggers and curators tools to share discoveries and get credit for them.

    ### Embedding Artist Cards

    Every artist page has an **Embed** button. Click it to get:
    - **iframe snippet** — paste directly into any HTML editor or CMS
    - **Script-tag snippet** — for blogs where iframes are restricted

    Both snippets automatically adapt to your blog's dark or light mode.

    To get attribution credit when you embed an artist, enter your blog handle in the "Your handle" field before copying the script-tag snippet. When readers load your page, Mercury records you as a curator for that artist.

    **QR Code**: Click "QR Code" to generate a QR code for the embed URL — useful for print materials or event flyers.

    ### Collection Embeds

    Collection embeds (`/embed/collection/[id]`) are available in the desktop app when sharing a public collection. The web version shows a placeholder directing visitors to download Mercury.

    ### RSS Feeds

    Subscribe to any artist, tag, or curator in your RSS reader:

    | What | RSS URL |
    |------|---------|
    | Artist updates | `/api/rss/artist/[slug]` |
    | Tag discovery | `/api/rss/tag/[tag-name]` |
    | Curator's picks | `/api/rss/curator/[handle]` |
    | New & Rising | `/api/rss/new-rising` |

    Append `?format=atom` to any feed URL for Atom format. Most feed readers accept both — try the default RSS first.

    Look for the RSS icon (orange square with signal arcs) on artist pages, the Discover page, and the New & Rising page.

    ### New & Rising

    The **New & Rising** page (link in the navigation) surfaces artists that Mercury doesn't see discussed elsewhere yet:

    - **Newly Active** — artists with recent activity dates
    - **Gaining Traction** — niche artists accumulating unusual tag combinations (the most interesting view)

    Use New & Rising as your weekly "what should I write about next" list.

    ### Curator Attribution

    When you embed an artist on your site (using the script-tag snippet with your handle), Mercury records you as a curator for that artist. Your handle then appears on the artist's Mercury page as "Discovered by @[yourhandle]".

    All curators who have featured an artist are credited — not just the first. Embedding the same artist on multiple posts still only records once per curator.

    Click your handle on any artist page to see all artists you've featured on Mercury.
    ```
  </action>
  <verify>
    `npm run check` passes with zero errors.
    ARCHITECTURE.md contains "Curator / Blog Tools" heading.
    docs/user-manual.md contains "Curator Tools" heading.
    Both docs reference the correct file paths and route names matching what was built in Plans 01-04.
  </verify>
  <done>
    ARCHITECTURE.md has Curator / Blog Tools section with embed, RSS, attribution, and New & Rising subsections + anti-patterns table. docs/user-manual.md has Curator Tools section with user-facing feature documentation. npm run check passes.
  </done>
</task>

</tasks>

<verification>
Run `npm run check` — zero TypeScript errors.
Run `npm run build` — clean build.
Start wrangler: `npx wrangler pages dev .svelte-kit/cloudflare --port 8788 --compatibility-flag=nodejs_compat`
- Visit /new-rising — two tabs with artist cards rendered
- Visit /api/new-rising → JSON with newArtists + gainingTraction arrays
- Visit /api/rss/new-rising → valid RSS XML with Content-Type: application/rss+xml
- New & Rising link visible in web nav
- ARCHITECTURE.md grep "Curator / Blog Tools" → found
- docs/user-manual.md grep "Curator Tools" → found
</verification>

<success_criteria>
New & Rising page renders two discovery views from D1 data. Curator filter via ?curator= URL param works. RSS feed at /api/rss/new-rising returns valid XML. Nav link added. ARCHITECTURE.md and user-manual.md updated with Phase 12 content. npm run check + build pass with zero errors.
</success_criteria>

<output>
After completion, create `.planning/phases/12-curator-blog-tools/12-04-SUMMARY.md`
</output>
