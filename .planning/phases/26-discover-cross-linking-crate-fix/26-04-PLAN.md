---
phase: 26-discover-cross-linking-crate-fix
plan: 04
type: execute
wave: 2
depends_on:
  - 26-01
  - 26-02
  - 26-03
files_modified:
  - tools/test-suite/manifest.mjs
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - XLINK-01
  - XLINK-02
  - XLINK-03
  - XLINK-04
  - XLINK-05
  - CRAT-01
must_haves:
  truths:
    - "Test manifest contains PHASE_26 export with P26-01 through P26-16 entries"
    - "All code-method tests pass when running node tools/test-suite/run.mjs --code-only --phase 26"
    - "PHASE_26 is included in the ALL_TESTS export at the bottom of manifest.mjs"
  artifacts:
    - path: "tools/test-suite/manifest.mjs"
      provides: "PHASE_26 test entries for all Phase 26 deliverables"
      contains: "PHASE_26"
  key_links:
    - from: "tools/test-suite/manifest.mjs"
      to: "ALL_TESTS"
      via: "...PHASE_26 spread"
      pattern: "PHASE_26"
---

<objective>
Add the Phase 26 test manifest entries and verify all code-method checks pass.

Purpose: Every phase ends with a test manifest update. The 16 tests from Plans 01-03 TEST-PLAN sections cover all 9 requirements via code-method checks, with UI interactions marked as skip.
Output: Updated manifest.mjs with PHASE_26, all code checks passing.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@tools/test-suite/manifest.mjs
@.planning/phases/26-discover-cross-linking-crate-fix/26-01-PLAN.md
@.planning/phases/26-discover-cross-linking-crate-fix/26-02-PLAN.md
@.planning/phases/26-discover-cross-linking-crate-fix/26-03-PLAN.md
@.planning/phases/26-discover-cross-linking-crate-fix/26-01-SUMMARY.md
@.planning/phases/26-discover-cross-linking-crate-fix/26-02-SUMMARY.md
@.planning/phases/26-discover-cross-linking-crate-fix/26-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PHASE_26 test entries to manifest and run test suite</name>
  <files>tools/test-suite/manifest.mjs</files>
  <action>
    Read `tools/test-suite/manifest.mjs` to find: (a) the PHASE_25 export block (to add PHASE_26 after it), (b) the ALL_TESTS export at the bottom (to spread PHASE_26 into it), and (c) the exact `fileContains` / `fileExists` / `anyFileContains` helper patterns used by other code tests.

    **Add PHASE_26 export after PHASE_25:**

    ```js
    // ---------------------------------------------------------------------------
    // PHASE 26 — Discover + Cross-Linking + Crate Fix
    // ---------------------------------------------------------------------------

    export const PHASE_26 = [
      {
        id: 'P26-01', phase: 26, area: 'Discover',
        desc: 'Discover page has a filter panel (discover-filter-panel or discover-layout class)',
        method: 'code',
        fn: () => fileContains('src/routes/discover/+page.svelte', 'discover-filter-panel') ||
                  fileContains('src/routes/discover/+page.svelte', 'discover-layout'),
      },
      {
        id: 'P26-02', phase: 26, area: 'Discover',
        desc: 'Discover +page.ts reads country from URL params',
        method: 'code',
        fn: () => fileContains('src/routes/discover/+page.ts', 'country'),
      },
      {
        id: 'P26-03', phase: 26, area: 'Discover',
        desc: 'Discover +page.ts reads era from URL params',
        method: 'code',
        fn: () => fileContains('src/routes/discover/+page.ts', 'era'),
      },
      {
        id: 'P26-04', phase: 26, area: 'Discover',
        desc: 'queries.ts exports getDiscoveryArtists function',
        method: 'code',
        fn: () => fileContains('src/lib/db/queries.ts', 'getDiscoveryArtists'),
      },
      {
        id: 'P26-05', phase: 26, area: 'Discover',
        desc: 'ArtistResult interface has uniqueness_score field',
        method: 'code',
        fn: () => fileContains('src/lib/db/queries.ts', 'uniqueness_score'),
      },
      {
        id: 'P26-06', phase: 26, area: 'Discover',
        desc: 'ArtistCard renders a uniqueness score bar',
        method: 'code',
        fn: () => fileContains('src/lib/components/ArtistCard.svelte', 'uniqueness-bar'),
      },
      {
        id: 'P26-07', phase: 26, area: 'Discover',
        desc: 'Discover page layout renders with filter panel and artist grid',
        method: 'skip',
        reason: 'Requires running desktop app — visual layout check',
      },
      {
        id: 'P26-08', phase: 26, area: 'Cross-Linking',
        desc: 'Artist page links to Style Map with ?tag= param',
        method: 'code',
        fn: () => fileContains('src/routes/artist/[slug]/+page.svelte', 'style-map?tag='),
      },
      {
        id: 'P26-09', phase: 26, area: 'Cross-Linking',
        desc: 'Style Map +page.ts reads ?tag= from URL params',
        method: 'code',
        fn: () => fileContains('src/routes/style-map/+page.ts', 'tag'),
      },
      {
        id: 'P26-10', phase: 26, area: 'Cross-Linking',
        desc: 'Scene page links to KB genre page',
        method: 'code',
        fn: () => fileContains('src/routes/scenes/[slug]/+page.svelte', '/kb/genre/'),
      },
      {
        id: 'P26-11', phase: 26, area: 'Cross-Linking',
        desc: 'Crate Dig result has Style Map cross-link',
        method: 'code',
        fn: () => fileContains('src/routes/crate/+page.svelte', 'style-map?tag='),
      },
      {
        id: 'P26-12', phase: 26, area: 'Cross-Linking',
        desc: 'Time Machine has Discover era cross-link',
        method: 'code',
        fn: () => fileContains('src/routes/time-machine/+page.svelte', 'discover?era='),
      },
      {
        id: 'P26-13', phase: 26, area: 'Cross-Linking',
        desc: 'Cross-links visible in desktop app with secondary text style',
        method: 'skip',
        reason: 'Requires running desktop app — visual verification',
      },
      {
        id: 'P26-14', phase: 26, area: 'Crate Fix',
        desc: 'Crate Dig country input no longer asks for raw ISO code',
        method: 'code',
        fn: () => !fileContains('src/routes/crate/+page.svelte', 'Country code (e.g. US, GB)')(),
      },
      {
        id: 'P26-15', phase: 26, area: 'Crate Fix',
        desc: 'Crate Dig has COUNTRIES list with named country options',
        method: 'code',
        fn: () => fileContains('src/routes/crate/+page.svelte', 'COUNTRIES'),
      },
      {
        id: 'P26-16', phase: 26, area: 'Crate Fix',
        desc: 'Crate Dig country filter shows dropdown of country names in desktop app',
        method: 'skip',
        reason: 'Requires running desktop app — UI dropdown verification',
      },
    ];
    ```

    **Important pattern note:** Looking at existing test patterns in the manifest, `fileContains(path, str)` returns a FUNCTION (not a boolean). Code tests use `fn: () => fileContains(...)()` (calling the returned function) or `fn: () => fileContains(...)` when the runner calls the fn and the fn returns the result function. Check the existing PHASE_25 entries to confirm the exact invocation pattern and match it exactly.

    From STATE.md: "[23-03] fileContains() in test runner returns a function, not a boolean — negation tests must call: !fileContains(path, str)()"

    So for P26-14 (negation test): `fn: () => !fileContains('src/routes/crate/+page.svelte', 'Country code (e.g. US, GB)')()`

    For positive tests: Match the PHASE_25 pattern exactly (check how P25-01 through P25-21 are written — if they use `fn: fileContains(...)` or `fn: () => fileContains(...)()`.

    **Add PHASE_26 to the ALL_TESTS spread** at the bottom of the file:
    Find the line like `...PHASE_25,` and add `...PHASE_26,` immediately after it.

    **Run the test suite after adding:**
    ```bash
    node tools/test-suite/run.mjs --code-only --phase 26
    ```

    All code-method tests (P26-01 through P26-06, P26-08 through P26-12, P26-14, P26-15) must pass. If any fail, diagnose and fix — either the test assertion needs correction, or the source file needs a fix. Debug before marking done.

    If a test fails because a source file pattern differs slightly from expected (e.g., `style-map?tag=` vs `style-map?tag =`), adjust the test to match the actual pattern in the file rather than fixing the source.
  </action>
  <verify>
    <automated>node tools/test-suite/run.mjs --code-only --phase 26</automated>
    <manual>Confirm: 13 code tests passing, 3 skipped, 0 failing for phase 26</manual>
  </verify>
  <done>node tools/test-suite/run.mjs --code-only --phase 26 reports all code tests passing (0 failures). manifest.mjs contains PHASE_26 export and it is included in ALL_TESTS.</done>
</task>

</tasks>

<verification>
Run `node tools/test-suite/run.mjs --code-only --phase 26` — all code tests pass.
Run `node tools/test-suite/run.mjs --code-only` — overall suite still passes (no regressions).
Verify: `tools/test-suite/manifest.mjs` contains `PHASE_26` and it is spread into the final `ALL_TESTS` export.
</verification>

<success_criteria>
- PHASE_26 exported from manifest.mjs with P26-01 through P26-16
- PHASE_26 spread into ALL_TESTS
- `node tools/test-suite/run.mjs --code-only --phase 26` passes all code checks
- Full suite `node tools/test-suite/run.mjs --code-only` shows no regressions
</success_criteria>

## TEST-PLAN

This plan IS the test manifest — it verifies the deliverables from Plans 01-03 by writing the test entries:
- P26-01 through P26-16 as defined in the task above

<output>
After completion, create `.planning/phases/26-discover-cross-linking-crate-fix/26-04-SUMMARY.md`
</output>
