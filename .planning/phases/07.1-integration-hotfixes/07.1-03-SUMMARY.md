---
phase: 07.1-integration-hotfixes
plan: 03
subsystem: ui
tags: [svelte, sveltekit, knowledge-base, discover, navigation]

# Dependency graph
requires:
  - phase: 07-knowledge-base
    provides: genre/scene detail pages with data.genre.mb_tag field
  - phase: 06-discovery-engine
    provides: Discover page with TagFilter consuming ?tags= query param
provides:
  - Genre/scene pages with conditional discover links at top and bottom
  - KB→Discover navigation bridge (GAP-04 closed)
affects: [kb-genre-page, discover-page, navigation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "{#if data.genre.mb_tag} guard prevents null mb_tag producing /discover?tags=null broken URLs"
    - "Dual-placement link pattern: inline top + footer bottom for maximum discoverability"

key-files:
  created: []
  modified:
    - src/routes/kb/genre/[slug]/+page.svelte

key-decisions:
  - "[Phase 07.1-03]: Discover links guarded by {#if data.genre.mb_tag} — genres table has nullable mb_tag, null would produce broken /discover?tags=null URL"
  - "[Phase 07.1-03]: Two link placements: inline after genre-header (top) and discover-footer as last element in genre-page (bottom) — per user decision for maximum visibility"
  - "[Phase 07.1-03]: Dynamic label 'Explore {data.genre.name} in Discover' (not hardcoded) — per user decision"
  - "[Phase 07.1-03]: No new data loading — data.genre.mb_tag already available from existing getGenreBySlug query in +page.server.ts"

patterns-established:
  - "Null-guard pattern: always wrap optional field links in {#if field} to prevent broken URLs"

requirements-completed: [KB-01, KB-02]

# Metrics
duration: 2min
completed: 2026-02-21
---

# Phase 07.1 Plan 03: Integration Hotfixes — Genre Discover Navigation Summary

**Conditional "Explore [Name] in Discover" links added to KB genre/scene pages at top (after header) and bottom (footer), closing the GAP-04 KB→Discover navigation bridge**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-21T14:29:55Z
- **Completed:** 2026-02-21T14:31:55Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added inline discover link immediately after `.genre-header` div — first thing users see after the genre title/meta
- Added `.discover-footer` block as the last element inside `.genre-page` div — bookend navigation anchor
- Both links guarded by `{#if data.genre.mb_tag}` — null mb_tag produces no link, not a broken `/discover?tags=null` URL
- Link URL follows `/discover?tags={data.genre.mb_tag}` — TagFilter on Discover page reads `?tags=` from `page.url.searchParams` automatically
- Dynamic label uses `data.genre.name` per user decision (not hardcoded generic text)
- Added `.discover-link` and `.discover-footer` CSS using design system custom properties

## Task Commits

Each task was committed atomically:

1. **Task 1: Add genre→discover navigation links (top and bottom)** - `dbd7700` (feat)

**Plan metadata:** (to be committed with SUMMARY.md and STATE.md)

## Files Created/Modified
- `src/routes/kb/genre/[slug]/+page.svelte` - Added discover links at top and bottom, plus CSS for .discover-link and .discover-footer

## Decisions Made
- `{#if data.genre.mb_tag}` guard on both links — RESEARCH.md Pitfall 4 warning: null mb_tag would produce `/discover?tags=null`, a broken URL returning no results
- Two placements (top + bottom) per user decision — maximum discoverability without cluttering the page
- Dynamic label `Explore {data.genre.name} in Discover` — genre name, not generic text
- No new server-side data loading needed — `data.genre.mb_tag` already returned by `getGenreBySlug` in `+page.server.ts`

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None - straightforward template and CSS addition. `npm run check` 0 errors, `npm run build` clean.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- KB→Discover navigation chain is complete: KB genre page → Discover filtered by that tag
- GAP-04 closed, KB-01 and KB-02 requirements satisfied
- Phase 07.1 integration hotfixes complete (all 3 plans done)

---
*Phase: 07.1-integration-hotfixes*
*Completed: 2026-02-21*
