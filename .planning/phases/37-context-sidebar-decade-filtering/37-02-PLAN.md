---
phase: 37-context-sidebar-decade-filtering
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/discover/+page.svelte
autonomous: true
requirements: []
must_haves:
  truths:
    - "The Discover filter panel shows 8 era decade pills: 50s, 60s, 70s, 80s, 90s, 00s, 10s, 20s"
    - "Clicking 50s activates the era=50s URL param and filters Discover results"
    - "Clicking 50s again deselects it (same toggle behavior as existing pills)"
    - "Existing era pills (60s-20s) continue to work identically"
  artifacts:
    - path: "src/routes/discover/+page.svelte"
      provides: "Decade row with 8 pills including 50s"
      contains: "'50s', '60s', '70s'"
  key_links:
    - from: "ERA_OPTIONS array"
      to: "toggleEra() function"
      via: "era-pill button onclick"
      pattern: "ERA_OPTIONS"
---

<objective>
Add '50s' to the beginning of ERA_OPTIONS in the Discover page, expanding the decade range from 60s-20s to 50s-20s. All toggle/URL/query logic is unchanged — a one-line addition.

Purpose: Extend discovery to cover 1950s music, completing the full modern era span (50s through 2020s).
Output: discover/+page.svelte with 8-item ERA_OPTIONS array.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-context-sidebar-decade-filtering/37-CONTEXT.md

<interfaces>
<!-- Exact current code from src/routes/discover/+page.svelte — confirmed from source -->

Current ERA_OPTIONS (line 12):
```typescript
const ERA_OPTIONS = ['60s', '70s', '80s', '90s', '00s', '10s', '20s'];
```

Target ERA_OPTIONS:
```typescript
const ERA_OPTIONS = ['50s', '60s', '70s', '80s', '90s', '00s', '10s', '20s'];
```

The toggleEra, buildUrl, and era-pill template are unchanged — they handle any string value in ERA_OPTIONS correctly.

Era pill template (lines 167-181):
```svelte
<div class="filter-section">
  <span class="filter-label">Era</span>
  <div class="era-pills">
    {#each ERA_OPTIONS as era}
      <button
        class="era-pill"
        class:active={data.era === era}
        onclick={() => toggleEra(era)}
      >
        {era}
      </button>
    {/each}
  </div>
</div>
```
The existing era-pills CSS uses flex-wrap: wrap — 8 pills at this size wrap to two rows of 4, which is visually balanced. No CSS changes needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add '50s' to ERA_OPTIONS in discover/+page.svelte</name>
  <files>src/routes/discover/+page.svelte</files>
  <action>
Read the file. Find the line:
```typescript
const ERA_OPTIONS = ['60s', '70s', '80s', '90s', '00s', '10s', '20s'];
```
Replace it with:
```typescript
const ERA_OPTIONS = ['50s', '60s', '70s', '80s', '90s', '00s', '10s', '20s'];
```

That is the only change. No other modifications to this file.
  </action>
  <verify>
    <automated>cd D:/Projects/BlackTape && npm run check 2>&1 | tail -10</automated>
  </verify>
  <done>ERA_OPTIONS array has 8 entries starting with '50s'. TypeScript check passes. Decade row on Discover page shows 50s pill alongside existing pills. Clicking 50s activates era=50s URL param; clicking again deselects.</done>
</task>

</tasks>

<verification>
npm run check passes with no errors.
Discover page /discover shows 8 era pills in the filter panel.
50s pill toggles era=50s URL param correctly.
</verification>

<success_criteria>
discover/+page.svelte ERA_OPTIONS = ['50s', '60s', '70s', '80s', '90s', '00s', '10s', '20s'].
TypeScript compiles without errors.
No other changes to the file.
</success_criteria>

<output>
After completion, create `.planning/phases/37-context-sidebar-decade-filtering/37-02-SUMMARY.md`
</output>
