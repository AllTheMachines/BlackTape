---
phase: 28-ux-cleanup-scope-reduction
plan: 05
type: execute
wave: 2
depends_on:
  - "28-02"
files_modified:
  - src/lib/components/AiSettings.svelte
  - src/routes/artist/[slug]/+page.svelte
autonomous: true
requirements:
  - POLISH-29
  - POLISH-32

must_haves:
  truths:
    - "The AI provider selector in AiSettings clearly shows which provider is selected and what it requires, without confusion about local vs remote AI"
    - "The artist page has per-platform social sharing buttons for Twitter/X and Bluesky, in addition to the existing Mastodon share"
  artifacts:
    - path: "src/lib/components/AiSettings.svelte"
      provides: "Redesigned AI provider section with clear grouping and status"
      contains: "provider-card"
    - path: "src/routes/artist/[slug]/+page.svelte"
      provides: "Twitter/X and Bluesky share buttons alongside existing Mastodon share"
      contains: "bluesky"
  key_links:
    - from: "src/lib/components/AiSettings.svelte"
      to: "AI_PROVIDERS array + provider selection state"
      via: "provider-card component redesign"
      pattern: "provider-card"
    - from: "src/routes/artist/[slug]/+page.svelte"
      to: "Share URL patterns for Twitter/X and Bluesky"
      via: "share button anchor elements"
      pattern: "bsky.app|twitter.com"
---

<objective>
Two polish items: (1) Redesign the AI provider selector in Settings to be clearer — show providers as cards with status, not a confusing list. (2) Add Twitter/X and Bluesky share buttons to the artist page alongside the existing Mastodon share.

Purpose: Polish #29 — current provider selector is confusing (which one is active? what does "badge" mean?). Polish #32 — Mastodon-only sharing misses most users; add mainstream options but keep Mastodon first.
Output: AI settings that explain themselves. Artist page share row with 3 platform options.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ux-cleanup-scope-reduction/28-CONTEXT.md

<interfaces>
<!-- From src/lib/components/AiSettings.svelte — current AI provider section: -->
```svelte
<!-- AI Auto-News: Provider Selection section (~line 366-397) -->
<div class="settings-section">
  <h3 class="settings-section-title">AI Summary Provider</h3>
  <p class="settings-hint">Choose which AI provider generates artist summaries...</p>
  <div class="provider-list">
    {#each AI_PROVIDERS as provider (provider.id)}
      <button class="provider-option"
        class:provider-option--selected={aiState.selectedProviderName === provider.id}
        onclick={() => handleProviderSelect(provider.id)}>
        <span class="provider-label">{provider.label}</span>
        {#if provider.badge}
          <span class="provider-badge">{provider.badge}</span>
        {/if}
      </button>
    {/each}
  </div>
  {#if selectedProvider}
    <p class="provider-instructions">{selectedProvider.instructions}</p>
    {#if selectedProvider.affiliateUrl}
      <button class="provider-affiliate-btn" onclick={...}>Get API key ↗</button>
    {/if}
  {/if}
</div>
```

<!-- From src/lib/ai/providers.ts: -->
```typescript
export interface AiProvider {
  id: string;
  label: string;
  badge?: string;           // e.g. "Free", "Paid"
  instructions: string;    // setup instructions shown after selection
  defaultModel: string;    // pre-fills model field
  baseUrl: string;         // pre-fills API base URL
  affiliateUrl?: string;   // link to get API key
}
export const AI_PROVIDERS: readonly AiProvider[]  // array of providers
export function getProviderById(id: string): AiProvider | undefined
```

<!-- From src/routes/artist/[slug]/+page.svelte — existing Mastodon share: -->
```svelte
let mastodonShareUrl = $derived(
  `https://sharetomastodon.github.io/?text=${encodeURIComponent(...)}`
);
// Used in template as: <a href={mastodonShareUrl} ...>↑</a>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign AI provider selector for clarity (#29)</name>
  <files>src/lib/components/AiSettings.svelte</files>
  <action>
Redesign the "AI Summary Provider" section in `src/lib/components/AiSettings.svelte` to use a card-based layout that shows each provider's status clearly.

**Replacement HTML for the provider list section (replace the entire `<!-- AI Auto-News: Provider Selection -->` settings-section div):**

```svelte
<!-- AI Summary Provider — redesigned for clarity (#29) -->
<div class="settings-section">
  <h3 class="settings-section-title">AI Summary Provider</h3>
  <p class="settings-hint">
    Select the AI service that generates artist summaries. Each provider requires an API key.
    Local AI (above) generates embeddings and recommendations — this provider is for text summaries only.
  </p>

  <div class="provider-grid">
    {#each AI_PROVIDERS as provider (provider.id)}
      {@const isSelected = aiState.selectedProviderName === provider.id}
      <button
        class="provider-card"
        class:provider-card--selected={isSelected}
        onclick={() => handleProviderSelect(provider.id)}
        type="button"
      >
        <div class="provider-card-header">
          <span class="provider-card-name">{provider.label}</span>
          {#if provider.badge}
            <span class="provider-card-badge">{provider.badge}</span>
          {/if}
          {#if isSelected}
            <span class="provider-card-check">✓</span>
          {/if}
        </div>
        <p class="provider-card-hint">{provider.instructions}</p>
        {#if provider.affiliateUrl && isSelected}
          <button
            class="provider-card-key-btn"
            type="button"
            onclick={(e) => { e.stopPropagation(); openAffiliateUrl(provider.affiliateUrl!); }}
          >
            Get API key ↗
          </button>
        {/if}
      </button>
    {/each}
  </div>

  {#if aiState.selectedProviderName && aiState.status === 'ready'}
    <p class="provider-status-ready">✓ Connected and ready</p>
  {/if}
</div>
```

**CSS to add in `<style>` block (replace or add alongside existing `.provider-option` styles):**

```css
.provider-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
  gap: var(--space-sm, 8px);
  margin-top: var(--space-sm, 8px);
}

.provider-card {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: var(--space-sm, 8px) var(--space-md, 12px);
  background: var(--bg-surface, var(--bg-4));
  border: 1px solid var(--border-subtle, var(--b-2));
  border-radius: var(--card-radius, var(--r));
  cursor: pointer;
  text-align: left;
  transition: border-color 0.1s, background 0.1s;
}

.provider-card:hover {
  border-color: var(--acc);
  background: var(--bg-3, #1a1a1a);
}

.provider-card--selected {
  border-color: var(--acc);
  background: var(--bg-3, #1a1a1a);
}

.provider-card-header {
  display: flex;
  align-items: center;
  gap: 6px;
}

.provider-card-name {
  font-size: 12px;
  font-weight: 600;
  color: var(--t-1, var(--text-primary));
  flex: 1;
}

.provider-card-badge {
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  padding: 1px 5px;
  background: var(--bg-4);
  color: var(--t-3);
  border: 1px solid var(--b-2);
  border-radius: var(--r);
}

.provider-card-check {
  font-size: 11px;
  color: var(--acc);
  font-weight: 700;
}

.provider-card-hint {
  font-size: 10px;
  color: var(--t-3, var(--text-secondary));
  line-height: 1.4;
  margin: 0;
}

.provider-card-key-btn {
  font-size: 10px;
  color: var(--acc);
  background: none;
  border: none;
  cursor: pointer;
  padding: 2px 0;
  text-align: left;
  font-family: inherit;
}

.provider-card-key-btn:hover {
  text-decoration: underline;
}

.provider-status-ready {
  margin-top: 8px;
  font-size: 11px;
  color: var(--t-3);
}
```

Remove the old `.provider-list`, `.provider-option`, `.provider-option--selected`, `.provider-badge`, `.provider-instructions`, `.provider-affiliate-btn` CSS rules that are replaced by the new `.provider-card-*` rules.

Note: The `provider-instructions` paragraph and `provider-affiliate-btn` button that existed OUTSIDE the loop (shown for the selected provider only) are now INSIDE the card. Remove those old elements from outside the loop.
  </action>
  <verify>
    <automated>npm run check --prefix D:/Projects/Mercury 2>&1 | tail -5</automated>
  </verify>
  <done>`npm run check` passes. AiSettings.svelte contains `provider-card` class and the grid layout. Old `provider-option` elements are replaced.</done>
</task>

<task type="auto">
  <name>Task 2: Add Twitter/X and Bluesky share buttons to artist page (#32)</name>
  <files>src/routes/artist/[slug]/+page.svelte</files>
  <action>
In `src/routes/artist/[slug]/+page.svelte`, add Twitter/X and Bluesky share URLs alongside the existing Mastodon share. Keep Mastodon as the primary/first option per project values.

**In the `<script>` block, find the `mastodonShareUrl` derived and add two more:**

```typescript
/** Twitter/X share URL */
let twitterShareUrl = $derived(
  `https://twitter.com/intent/tweet?text=${encodeURIComponent(`${data.artist.name} — discovered on BlackTape`)}&url=${encodeURIComponent($page.url.href)}`
);

/** Bluesky share URL */
let bskyShareUrl = $derived(
  `https://bsky.app/intent/compose?text=${encodeURIComponent(`${data.artist.name} — discovered on BlackTape ${$page.url.href}`)}`
);
```

**In the HTML template, find the existing Mastodon share button/link (search for `mastodonShareUrl` in the template). It currently renders as something like:**
```svelte
<a href={mastodonShareUrl} target="_blank" rel="noopener noreferrer" class="share-btn" title="Share on Mastodon">↑</a>
```

Replace it with a share row containing all three platforms. Add a tooltip/title to each:

```svelte
<div class="share-row">
  <a href={mastodonShareUrl} target="_blank" rel="noopener noreferrer" class="share-btn share-btn--mastodon" title="Share on Mastodon">
    <span class="share-icon">🐘</span>
  </a>
  <a href={bskyShareUrl} target="_blank" rel="noopener noreferrer" class="share-btn share-btn--bsky" title="Share on Bluesky">
    <span class="share-icon">☁</span>
  </a>
  <a href={twitterShareUrl} target="_blank" rel="noopener noreferrer" class="share-btn share-btn--twitter" title="Share on Twitter/X">
    <span class="share-icon">✕</span>
  </a>
</div>
```

**CSS to add:**
```css
.share-row {
  display: flex;
  gap: 4px;
  align-items: center;
}

.share-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 26px;
  height: 26px;
  background: var(--bg-4);
  border: 1px solid var(--b-2);
  border-radius: var(--r);
  text-decoration: none;
  font-size: 12px;
  transition: border-color 0.1s, background 0.1s;
}

.share-btn:hover {
  background: var(--bg-3);
  border-color: var(--b-3);
}

.share-icon {
  line-height: 1;
}
```

**Important:** Read the actual location of the Mastodon share button in the template first (search for `mastodonShareUrl` in the svelte template section). The replacement must go in the exact same location. Do not add `.share-btn` CSS if there is already a `.share-btn` style defined in this file — check first and merge if needed.

Note: The `$page` store is already imported in this file (line: `import { page } from '$app/stores';`).
  </action>
  <verify>
    <automated>grep -c "bskyShareUrl\|twitterShareUrl" D:/Projects/Mercury/src/routes/artist/\[slug\]/+page.svelte</automated>
  </verify>
  <done>Artist page contains `bskyShareUrl` and `twitterShareUrl` derived values and uses them in share buttons. `npm run check` passes.</done>
</task>

</tasks>

<verification>
Run `npm run check` from `D:/Projects/Mercury`. Both files must compile without TypeScript or Svelte errors.
</verification>

<success_criteria>
- `npm run check` exits 0
- `src/lib/components/AiSettings.svelte` contains `provider-card` and `provider-grid` classes
- `src/routes/artist/[slug]/+page.svelte` contains `bskyShareUrl` and `twitterShareUrl` derived values
- Old `provider-option` CSS replaced with new `provider-card` CSS
</success_criteria>

<output>
After completion, create `.planning/phases/28-ux-cleanup-scope-reduction/28-05-SUMMARY.md`
</output>
