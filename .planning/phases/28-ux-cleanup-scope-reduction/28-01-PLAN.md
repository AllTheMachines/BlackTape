---
phase: 28-ux-cleanup-scope-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/LeftSidebar.svelte
  - src/routes/scenes/+page.svelte
  - src/routes/room/[channelId]/+page.svelte
autonomous: true
requirements:
  - SCOPE-01
  - SCOPE-02
  - SCOPE-03

must_haves:
  truths:
    - "Scenes, Listening Rooms, and ActivityPub/DMs do not appear as nav items in the left sidebar"
    - "All three routes still function when navigated to directly"
    - "Users landing on a hidden route see a 'coming in v2' notice"
  artifacts:
    - path: "src/lib/components/LeftSidebar.svelte"
      provides: "Nav with 3 deferred links removed from navGroups"
      contains: "coming in v2"
    - path: "src/routes/scenes/+page.svelte"
      provides: "Coming-in-v2 notice on Scenes page"
    - path: "src/routes/room/[channelId]/+page.svelte"
      provides: "Coming-in-v2 notice on Rooms page"
  key_links:
    - from: "src/lib/components/LeftSidebar.svelte"
      to: "navGroups array"
      via: "Remove scenes, rooms, fediverse nav entries"
      pattern: "scenes|room|fediverse|ActivityPub"
---

<objective>
Remove Scenes, Listening Rooms, and ActivityPub/DMs from the left navigation bar. Keep routes working but hidden from nav — power users with bookmarks still reach them. Add a small "coming in v2" note on each deferred page.

Purpose: Simplify what new users see in v1.5. Full features return in v2.
Output: Cleaner nav, 3 deferred pages with honest "coming in v2" messaging.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-ux-cleanup-scope-reduction/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deferred nav items from LeftSidebar</name>
  <files>src/lib/components/LeftSidebar.svelte</files>
  <action>
In `src/lib/components/LeftSidebar.svelte`, remove three entries from `navGroups`:

1. Remove `{ href: '/scenes', label: 'Scenes', icon: '◎' }` from the Discover group.
2. Remove the Listening Rooms entry — search for `room` in the links array (it appears as `/room` or similar).
3. Remove the ActivityPub / DMs entry — search for `/profile` or fediverse-related nav entries. The Nostr/DMs link should also be removed if present.

Current navGroups structure (from source):
```typescript
const navGroups = [
  {
    label: 'Discover',
    links: [
      { href: '/discover', label: 'Discover', icon: '◉' },
      { href: '/style-map', label: 'Style Map', icon: '⬡' },
      { href: '/kb', label: 'Knowledge Base', icon: '◈' },
      { href: '/time-machine', label: 'Time Machine', icon: '◷' },
      { href: '/crate', label: 'Crate Dig', icon: '▦' },
      { href: '/scenes', label: 'Scenes', icon: '◎' }  // REMOVE THIS
    ]
  },
  {
    label: 'Library',
    links: [
      { href: '/library', label: 'Library', icon: '▤' },
      { href: '/explore', label: 'Explore', icon: '◬' }
    ]
  },
  {
    label: 'Account',
    links: [
      { href: '/profile', label: 'Profile', icon: '◐' },
      { href: '/settings', label: 'Settings', icon: '⚙' },
      { href: '/about', label: 'About', icon: '◌' }
    ]
  }
] as const;
```

Remove only the Scenes link from Discover. Search the file for any `room` or `fediverse` or `ActivityPub` or DMs-related nav entry — if found, remove those too. If none found (rooms and ActivityPub may not be in nav), that is fine.

Do NOT change any routes, imports, or logic. Only modify the navGroups array to remove deferred items.
  </action>
  <verify>
    <automated>grep -c "scenes" D:/Projects/Mercury/src/lib/components/LeftSidebar.svelte || true</automated>
  </verify>
  <done>The Scenes link is no longer in navGroups. Any rooms/ActivityPub nav entries are also removed. Remaining nav items are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add "coming in v2" notice to deferred pages</name>
  <files>src/routes/scenes/+page.svelte</files>
  <action>
In `src/routes/scenes/+page.svelte`, add a small "coming in v2" notice banner near the top of the page template (above the main content). Style it using existing design tokens.

Place this block at the very top of the page `<div>` container (after any `<svelte:head>`):

```svelte
<div class="v2-notice">
  <span class="v2-badge">Coming in v2</span>
  Scenes are being redesigned for a better community experience. Check back in the next major release.
</div>
```

Add minimal CSS scoped to this component:
```css
.v2-notice {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 16px;
  background: var(--bg-4);
  border-bottom: 1px solid var(--b-1);
  font-size: 11px;
  color: var(--t-3);
}
.v2-badge {
  background: var(--acc);
  color: var(--bg-1);
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  padding: 2px 6px;
  border-radius: var(--r);
  white-space: nowrap;
  flex-shrink: 0;
}
```

Also find the room route — `src/routes/room/[channelId]/+page.svelte`. Add the same notice banner but with text: "Listening Rooms are being redesigned for a better community experience. Check back in the next major release."

If there is no `src/routes/room/[channelId]/+page.svelte` file, check `src/routes/room/+page.svelte` — add the notice to whichever file exists.

Do NOT modify any existing functionality or imports in either file.
  </action>
  <verify>
    <automated>grep -l "v2-notice" D:/Projects/Mercury/src/routes/scenes/+page.svelte D:/Projects/Mercury/src/routes/room/\[channelId\]/+page.svelte 2>/dev/null || grep -l "v2-notice" D:/Projects/Mercury/src/routes/scenes/+page.svelte 2>/dev/null</automated>
  </verify>
  <done>Scenes page has v2-notice banner. Room page (whichever file exists) has v2-notice banner with appropriate message. Both pages still render their existing content below the notice.</done>
</task>

</tasks>

<verification>
Run `npm run check` from `D:/Projects/Mercury` to verify TypeScript + Svelte compilation passes. The LeftSidebar must compile with the Scenes entry removed.
</verification>

<success_criteria>
- `npm run check` exits 0
- LeftSidebar.svelte no longer contains `/scenes` in navGroups
- Scenes page contains "v2-notice" class
- Room page contains "v2-notice" class
- All existing nav items (Discover, Style Map, KB, Time Machine, Crate Dig, Library, Explore, Profile, Settings, About) remain intact
</success_criteria>

<output>
After completion, create `.planning/phases/28-ux-cleanup-scope-reduction/28-01-SUMMARY.md`
</output>
