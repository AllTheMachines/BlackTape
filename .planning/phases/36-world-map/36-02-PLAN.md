---
phase: 36-world-map
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/+layout.svelte
autonomous: true
requirements: []
must_haves:
  truths:
    - "Navigating to /world-map suppresses the left sidebar, ControlBar, and footer"
    - "Titlebar and Player remain visible on the world-map route"
    - "World Map nav item appears in the top nav alongside Rabbit Hole"
    - "npm run check passes after layout changes"
  artifacts:
    - path: "src/routes/+layout.svelte"
      provides: "isWorldMap derived variable + world-map branch in template + nav item"
      contains: "isWorldMap"
  key_links:
    - from: "src/routes/+layout.svelte"
      to: "/world-map"
      via: "$page.url.pathname.startsWith('/world-map')"
      pattern: "isWorldMap"
---

<objective>
Add the `isWorldMap` layout bypass to the root layout (`src/routes/+layout.svelte`) and insert the "World Map" nav item. The World Map gets the same full-viewport immersive treatment as Rabbit Hole — only Titlebar and Player, no cockpit shell.

Purpose: Ensures the map fills the viewport without any sidebar, ControlBar, or nav chrome getting in the way.
Output: Modified `+layout.svelte` with isWorldMap bypass and nav item.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-world-map/36-CONTEXT.md
@.planning/phases/36-world-map/36-RESEARCH.md

<interfaces>
<!-- From src/routes/+layout.svelte (current state — read before editing) -->
<!-- Key pattern to mirror: isRabbitHole at line 38 -->
```svelte
let isEmbed = $derived($page.url.pathname.startsWith('/embed'));
let isRabbitHole = $derived($page.url.pathname.startsWith('/rabbit-hole'));
```

<!-- Current template structure (three branches) -->
```svelte
{#if isEmbed}
  {@render children()}
{:else if isRabbitHole}
  <!-- Titlebar + children + Player — NO cockpit shell -->
  {#if tauriMode}
    <Titlebar />
    <UpdateBanner />
    <CriticalUpdateModal />
  {/if}
  {@render children()}
  {#if isTauri()}
    <Player />
  {/if}
{:else}
  <!-- Standard cockpit layout (ControlBar, PanelLayout, etc.) -->
{/if}
```

<!-- Current Tauri nav links (lines 197-202 in +layout.svelte) -->
```svelte
<a href="/discover" class="nav-link">Discover</a>
<a href="/rabbit-hole" class="nav-link" class:active={...}>Rabbit Hole</a>
<a href="/library" class="nav-link">Library</a>
<a href="/explore" class="nav-link">Explore</a>
<a href="/profile" class="nav-link" class:active={...}>Profile</a>
<a href="/settings" class="nav-link">Settings</a>
<a href="/about" class="nav-link">About</a>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isWorldMap bypass and nav item to root layout</name>
  <files>src/routes/+layout.svelte</files>
  <action>
Read `src/routes/+layout.svelte` first — it is 455 lines and must be preserved exactly except for these targeted changes.

**Change 1: Add isWorldMap derived variable** (after the existing `isRabbitHole` line, around line 38):
```svelte
let isRabbitHole = $derived($page.url.pathname.startsWith('/rabbit-hole'));
let isWorldMap = $derived($page.url.pathname.startsWith('/world-map'));
```

**Change 2: Add isWorldMap branch in the template** (insert a new `{:else if isWorldMap}` block between the `{:else if isRabbitHole}` block and the `{:else}` standard cockpit block):
```svelte
{:else if isRabbitHole}
  {#if tauriMode}
    <Titlebar />
    <UpdateBanner />
    <CriticalUpdateModal />
  {/if}
  {@render children()}
  {#if isTauri()}
    <Player />
  {/if}

{:else if isWorldMap}

{#if tauriMode}
  <Titlebar />
  <UpdateBanner />
  <CriticalUpdateModal />
{/if}

{@render children()}

{#if isTauri()}
  <Player />
{/if}

{:else}
  <!-- Standard cockpit layout... -->
```

This is the SAME structure as isRabbitHole — no cockpit shell, no nav, no footer, just Titlebar + children + Player.

**Change 3: Add World Map nav item in the Tauri nav** (insert after the Rabbit Hole nav link):
```svelte
<a href="/rabbit-hole" class="nav-link" class:active={$page.url.pathname.startsWith('/rabbit-hole')}>Rabbit Hole</a>
<a href="/world-map" class="nav-link" class:active={$page.url.pathname.startsWith('/world-map')}>World Map</a>
<a href="/library" class="nav-link">Library</a>
```

Do NOT add a World Map link to the non-Tauri (web) nav — the web nav is a different block (lines 221-230) for non-Tauri routes and World Map is Tauri-only (requires SQLite).

Preserve all other layout code exactly as-is.
  </action>
  <verify>
    <automated>cd D:/Projects/BlackTape && npm run check 2>&1 | tail -20</automated>
  </verify>
  <done>`isWorldMap` variable exists, template has the new branch, "World Map" nav link present. `npm run check` passes.</done>
</task>

</tasks>

<verification>
- `grep -n "isWorldMap" src/routes/+layout.svelte` shows the derived variable and template usage
- `grep -n "World Map" src/routes/+layout.svelte` shows the nav link
- `npm run check` passes
</verification>

<success_criteria>
Root layout has isWorldMap bypass (same treatment as Rabbit Hole). World Map nav item appears after Rabbit Hole in Tauri nav. Type check passes.
</success_criteria>

<output>
After completion, create `.planning/phases/36-world-map/36-02-SUMMARY.md`
</output>
