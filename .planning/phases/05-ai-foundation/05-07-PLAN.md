---
phase: 05-ai-foundation
plan: 07
type: execute
wave: 4
depends_on: ["05-04", "05-05", "05-06"]
files_modified:
  - ARCHITECTURE.md
  - docs/user-manual.md
autonomous: false

must_haves:
  truths:
    - "AI opt-in toggle works: off by default, downloads models on enable"
    - "Model download shows progress and completes successfully"
    - "llama-server starts after download and health check passes"
    - "Explore page accepts NL queries and returns curated recommendations"
    - "Explore refinement works for at least 2 follow-up queries"
    - "Favorite button on artist pages toggles and persists"
    - "Recommendations appear on artist pages after enough favorites saved"
    - "AI summary generates when Wikipedia bio is unavailable"
    - "Taste editor shows tags and allows weight adjustment"
    - "Settings persist across app restart"
    - "Web build unaffected — no AI components shown, no errors"
    - "Documentation (ARCHITECTURE.md, user-manual.md) updated to cover AI features"
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "Updated technical architecture including AI subsystem"
    - path: "docs/user-manual.md"
      provides: "Updated user manual with AI features documentation"
  key_links:
    - from: "src/routes/+layout.svelte"
      to: "src/lib/ai/state.svelte.ts"
      via: "AI initialization on app start"
      pattern: "initializeAi|loadAiSettings"
    - from: "src/routes/explore/+page.svelte"
      to: "src/lib/ai/engine.ts"
      via: "NL query through AI provider"
      pattern: "getAiProvider"
    - from: "src/lib/components/AiRecommendations.svelte"
      to: "src/lib/taste/profile.svelte.ts"
      via: "taste threshold check"
      pattern: "hasEnoughData"
---

<objective>
End-to-end verification of all Phase 5 AI features plus documentation updates. Human verifies the complete AI experience from opt-in through usage.

Purpose: Verify that all AI features work together as a cohesive system. Update ARCHITECTURE.md and docs/user-manual.md to document the new AI subsystem (mandatory per project rules).

Output: Verified AI features, updated documentation.
</objective>

<execution_context>
@C:\Users\User\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\User\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@ARCHITECTURE.md
@docs/user-manual.md
@.planning/phases/05-ai-foundation/05-04-SUMMARY.md
@.planning/phases/05-ai-foundation/05-05-SUMMARY.md
@.planning/phases/05-ai-foundation/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ARCHITECTURE.md and docs/user-manual.md</name>
  <files>
    ARCHITECTURE.md
    docs/user-manual.md
  </files>
  <action>
    **ARCHITECTURE.md:** Add a new "AI Subsystem" section documenting:
    - Architecture overview: llama-server sidecar, AI provider interface, taste profile
    - Component diagram: Tauri app → sidecar (llama-server) → local HTTP API → frontend AI provider
    - taste.db schema: ai_settings, taste_tags, taste_anchors, favorite_artists, artist_embeddings
    - Model management: opt-in download, sidecar lifecycle, health polling
    - Provider pattern: local (llama-server) vs remote (OpenAI-compatible API)
    - Integration points: artist page recommendations, explore page NL queries, AI summaries, taste signals

    **docs/user-manual.md:** Add documentation for AI features:
    - Enabling AI: Settings → AI Features toggle → model download
    - Explore page: How to use NL queries, refinement, what to expect
    - Recommendations: How they appear on artist pages, why they require favorites first
    - Favorites: How to save artists, how they affect recommendations
    - Taste profile editing: Tag weights, artist anchors
    - Remote API: How to configure an alternative API provider
    - Privacy: All processing local, data never leaves the machine
    - Troubleshooting: AI not loading (check settings), no recommendations (need more favorites), llama-server issues
  </action>
  <verify>
    Read ARCHITECTURE.md and docs/user-manual.md to confirm AI sections exist and are accurate.
  </verify>
  <done>
    Documentation updated with full AI subsystem coverage in both ARCHITECTURE.md and user-manual.md.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete AI Foundation — opt-in flow, NL explore, artist recommendations, summaries, taste profiling, and taste editing</what-built>
  <how-to-verify>
    **Prerequisites:**
    - Run the Tauri dev build: `cd src-tauri && cargo tauri dev`
    - Ensure internet connection (for model download from HuggingFace)

    **Test 1: AI Opt-in Flow**
    1. Navigate to /settings
    2. Verify AI Features toggle is OFF by default
    3. Toggle ON — should show download prompt with ~900MB total
    4. Click "Download Models" — verify progress bar shows for each model
    5. After download, verify AI status in header transitions to "ready"

    **Test 2: Explore Page**
    1. Navigate to /explore (via header nav link)
    2. Type: "find me something like Boards of Canada but darker"
    3. Verify numbered results appear with artist names and descriptions
    4. Click a matched artist name — should navigate to artist page
    5. Type refinement: "more electronic"
    6. Verify results update
    7. Type another refinement to confirm conversation context works

    **Test 3: Artist Page Features**
    1. Navigate to any artist page (e.g., search for an artist)
    2. Verify favorite (heart) button appears in header
    3. Click to save as favorite — heart should fill
    4. Click again to unfavorite — heart should hollow
    5. Save at least 5 different artists as favorites
    6. Return to an artist page — verify "You might also like" section appears
    7. Navigate to an artist with no Wikipedia bio — verify AI summary generates

    **Test 4: Taste Profile**
    1. Navigate to /settings
    2. Verify "Taste Profile" section appears below AI settings
    3. Verify tags from favorited artists appear with weights
    4. Adjust a tag weight slider — verify value changes
    5. Add a manual tag — verify it appears with source="manual"
    6. Search and pin an artist anchor — verify it appears in the list

    **Test 5: Persistence**
    1. Close the app completely
    2. Reopen — verify AI is still enabled, favorites still saved, taste tags persist

    **Test 6: Web Build**
    1. Run `npm run dev` (web build, not Tauri)
    2. Verify no AI-related UI appears (no explore nav, no settings, no favorite buttons)
    3. Verify no console errors related to AI modules
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. ARCHITECTURE.md has AI Subsystem section
2. docs/user-manual.md has AI features documentation
3. All 6 verification tests pass
4. Web build has no AI-related regressions
</verification>

<success_criteria>
All Phase 5 AI features verified working end-to-end: opt-in flow downloads models and starts sidecar, explore page handles NL queries with refinement, artist pages show recommendations when taste data exists, favorites persist and feed taste profile, taste editing works, everything persists across restarts, web build is clean.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-foundation/05-07-SUMMARY.md`
</output>
