---
phase: 27-search-knowledge-base
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/kb/genre/[slug]/+page.svelte
autonomous: true
requirements:
  - KBAS-01

must_haves:
  truths:
    - "KB genre page shows a coloured type badge pill next to the genre title (e.g. 'Post-punk — Genre')"
    - "Key artists are shown as compact rows with name + tags, not full ArtistCard grid"
    - "Related genres show colour-coded type dots (genre=default, scene=amber, city=green)"
    - "Genre Map section shows a styled placeholder box 'Genre Map — Coming Soon' instead of the live graph"
    - "Description panel (Wikipedia or AI summary) is styled as an information panel with source badge"
  artifacts:
    - path: "src/routes/kb/genre/[slug]/+page.svelte"
      provides: "Redesigned KB genre page"
      contains: "genre-type-pill"
  key_links:
    - from: "src/routes/kb/genre/[slug]/+page.svelte"
      to: "data.genre.type"
      via: "genre-type-pill renders coloured badge based on type value"
      pattern: "genre-type-pill"
    - from: "genre map section"
      to: "placeholder"
      via: "GenreGraph replaced by genre-map-placeholder div"
      pattern: "genre-map-placeholder"
---

<objective>
Redesign the KB genre page to match the v1.4 design system and KBAS-01 spec.

Purpose: The KB genre page currently uses old styling (bare text badge, ArtistCard grid, live graph). The redesign aligns it with v1.4 design tokens, makes the type badge a coloured pill, shows key artists as compact rows, and replaces the live ForceGraph with a coming-soon placeholder.
Output: /kb/genre/[slug]/+page.svelte with type badge pill, compact artist rows, styled related genres, and genre map placeholder.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/routes/kb/genre/[slug]/+page.svelte
@src/routes/kb/genre/[slug]/+page.ts
@src/lib/styles/theme.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign KB genre page — type badge, compact artist rows, map placeholder</name>
  <files>src/routes/kb/genre/[slug]/+page.svelte</files>
  <action>
Rewrite the template and styles for /kb/genre/[slug]/+page.svelte. Keep all existing data loading logic (script section is unchanged). All changes are in the template markup and CSS.

**Type badge pill:** Replace the existing `<span class="genre-type-badge">` plain text badge with a styled coloured pill:
```svelte
<span class="genre-type-pill type-{data.genre.type}" data-testid="genre-type-pill">
  {data.genre.type.charAt(0).toUpperCase() + data.genre.type.slice(1)}
</span>
```

Position the pill inline next to the H1, not on its own line above it:
```svelte
<div class="genre-header">
  <div class="genre-title-row">
    <h1 data-testid="genre-title">{data.genre.name}</h1>
    <span class="genre-type-pill type-{data.genre.type}" data-testid="genre-type-pill">
      {data.genre.type.charAt(0).toUpperCase() + data.genre.type.slice(1)}
    </span>
  </div>
  {#if data.genre.inception_year}
    <p class="genre-meta">
      Est. {data.genre.inception_year}{data.genre.origin_city ? ` · ${data.genre.origin_city}` : ''}
    </p>
  {/if}
</div>
```

**Key artists — compact rows:** Replace the `<div class="artist-grid">` with `<ArtistCard>` loop to a compact list. Remove the ArtistCard import and replace with inline compact rows. Keep the ArtistCard import if needed elsewhere on the page, but in the Key Artists section use:
```svelte
<section class="genre-section" data-testid="key-artists-section">
  <h2>Key Artists</h2>
  <ul class="key-artists-list">
    {#each data.keyArtists.slice(0, 8) as artist}
      <li class="key-artist-row">
        <a href="/artist/{artist.slug}" class="key-artist-name">{artist.name}</a>
        {#if artist.tags}
          <span class="key-artist-tags">{artist.tags.split(', ').slice(0, 3).join(', ')}</span>
        {/if}
      </li>
    {/each}
  </ul>
</section>
```
This renders 6–8 artists (slice to 8) as compact rows with name + top 3 tags. Much denser than the card grid.

**Related genres — keep existing structure but ensure type dots are styled correctly.** The existing `.chip-type-dot.type-scene` and `.chip-type-dot.type-city` styles already exist. Add `type-genre` dot styling and update to use v1.4 tokens:
- `type-genre`: `background: var(--text-muted)` (neutral grey — genre is the default type)
- `type-scene`: `background: var(--acc, #c4a55a)` (amber — scenes are places/movements)
- `type-city`: `background: #3a8060` (green — cities are geographic)

Replace the old hex colour values with design token references where possible.

**Genre Map placeholder:** Replace the entire `{#if data.subgraph.nodes.length > 1}` block (which renders the live `<GenreGraph>` component) with a styled placeholder:
```svelte
<section class="genre-section genre-map-section" data-testid="genre-map-section">
  <h2>Genre Map</h2>
  <div class="genre-map-placeholder" data-testid="genre-map-placeholder">
    <span class="placeholder-label">Genre Map — Coming Soon</span>
    <p class="placeholder-hint">A visual map of how this genre connects to others is in development.</p>
  </div>
</section>
```

Remove the `import GenreGraph from '$lib/components/GenreGraph.svelte';` line since we're no longer rendering GenreGraph here.

Keep the `import ArtistCard from '$lib/components/ArtistCard.svelte';` — it is removed from the key artists section but can be removed entirely since it's no longer used.

Actually: remove both imports (GenreGraph and ArtistCard) since neither is used after this rewrite.

**Updated CSS** (replace old styles wholesale, keeping the same class names where they still apply):

```css
.genre-page {
  max-width: 860px;
  margin: 0 auto;
  padding: var(--space-lg, 1.5rem) var(--space-md, 1rem);
}

.genre-header {
  margin-bottom: var(--space-lg, 1.5rem);
}

.genre-title-row {
  display: flex;
  align-items: center;
  gap: var(--space-sm, 0.5rem);
  margin-bottom: var(--space-xs, 0.25rem);
}

.genre-title-row h1 {
  font-size: 1.8rem;
  font-weight: 700;
  margin: 0;
  color: var(--text-primary);
}

/* Type badge pill */
.genre-type-pill {
  display: inline-block;
  padding: 2px 8px;
  border-radius: var(--r, 2px);
  font-size: 0.65rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  border: 1px solid;
  /* Default (genre) */
  color: var(--text-muted);
  border-color: var(--border-default);
  background: transparent;
}

.genre-type-pill.type-scene {
  color: var(--acc, #c4a55a);
  border-color: var(--b-acc, rgba(196,165,90,0.3));
  background: var(--acc-bg, rgba(196,165,90,0.08));
}

.genre-type-pill.type-city {
  color: #4aad80;
  border-color: rgba(74, 173, 128, 0.3);
  background: rgba(74, 173, 128, 0.08);
}

.genre-meta {
  color: var(--text-muted);
  font-size: 0.85rem;
  margin: 0;
}

/* Description panel */
.genre-description {
  position: relative;
  padding: var(--space-md, 1rem);
  background: var(--bg-surface, #141414);
  border: 1px solid var(--border-subtle);
  border-radius: var(--r, 2px);
  margin-bottom: var(--space-lg, 1.5rem);
}

.genre-description p {
  margin: 0 0 var(--space-xs) 0;
  color: var(--text-secondary);
  font-size: 0.9rem;
  line-height: 1.6;
}

.source-badge {
  font-size: 0.6rem;
  text-transform: uppercase;
  color: var(--text-muted);
  letter-spacing: 0.1em;
  background: var(--bg-elevated);
  border: 1px solid var(--border-subtle);
  padding: 1px 5px;
  border-radius: var(--r, 2px);
}

.genre-cta {
  padding: var(--space-lg);
  text-align: center;
  color: var(--text-muted);
  font-size: 0.9rem;
}

.cta-invite a {
  color: var(--acc, #c4a55a);
}

.scene-map-section {
  margin-bottom: var(--space-lg);
}

/* Section layout */
.genre-section {
  margin-bottom: var(--space-xl, 2rem);
}

.genre-section h2 {
  font-size: 0.7rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-muted);
  margin: 0 0 var(--space-sm);
}

/* Compact key artists list */
.key-artists-list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 1px;
}

.key-artist-row {
  display: flex;
  align-items: baseline;
  gap: var(--space-sm);
  padding: var(--space-xs) var(--space-sm);
  background: var(--bg-surface);
  border: 1px solid var(--border-subtle);
}

.key-artist-row:hover {
  background: var(--bg-hover);
  border-color: var(--border-default);
}

.key-artist-name {
  font-weight: 500;
  color: var(--text-accent, var(--acc));
  text-decoration: none;
  font-size: 0.9rem;
  white-space: nowrap;
}

.key-artist-name:hover {
  text-decoration: underline;
}

.key-artist-tags {
  font-size: 0.75rem;
  color: var(--text-muted);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* Related genres */
.related-genres {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-xs);
}

.related-genre-chip {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 4px 10px;
  background: var(--bg-surface);
  border: 1px solid var(--border-subtle);
  border-radius: var(--r, 2px);
  text-decoration: none;
  font-size: 0.8rem;
  color: var(--text-secondary);
}

.related-genre-chip:hover {
  background: var(--bg-hover);
  border-color: var(--border-default);
  color: var(--text-primary);
}

.chip-type-dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  flex-shrink: 0;
  background: var(--text-muted);
}

.chip-type-dot.type-scene {
  background: var(--acc, #c4a55a);
}

.chip-type-dot.type-city {
  background: #4aad80;
}

/* Genre map placeholder */
.genre-map-section {
  min-height: 0;
}

.genre-map-placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: var(--space-xs);
  padding: var(--space-xl, 2rem);
  background: var(--bg-surface);
  border: 1px dashed var(--border-subtle);
  border-radius: var(--r, 2px);
  text-align: center;
}

.placeholder-label {
  font-size: 0.85rem;
  font-weight: 500;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}

.placeholder-hint {
  font-size: 0.75rem;
  color: var(--text-muted);
  max-width: 320px;
  margin: 0;
  opacity: 0.7;
}

/* Discover link */
.discover-link {
  display: inline-block;
  font-size: 0.8rem;
  color: var(--acc, #c4a55a);
  text-decoration: none;
  margin-bottom: var(--space-md);
}

.discover-link:hover {
  text-decoration: underline;
}

.discover-footer {
  margin-top: var(--space-xl);
  padding-top: var(--space-md);
  border-top: 1px solid var(--border-subtle);
  text-align: center;
}
```
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -5</automated>
  </verify>
  <done>KB genre page has genre-type-pill badge, compact key-artist-row list, colour-coded chip-type-dot for related genres, and genre-map-placeholder. GenreGraph and ArtistCard imports removed. npm run check passes.</done>
</task>

</tasks>

<verification>
npm run check passes.
Grep +page.svelte for: genre-type-pill, key-artist-row, genre-map-placeholder, data-testid="genre-type-pill", data-testid="genre-map-placeholder".
Verify GenreGraph import is removed.
</verification>

<success_criteria>
- Type badge is a coloured pill inline with the H1 title (genre=grey, scene=amber, city=green)
- Key artists show as compact rows (name + tags), 8 max, not ArtistCard cards
- Related genres have colour-coded type dots using v1.4 design tokens
- Genre Map section shows a styled placeholder box with dashed border
- GenreGraph component no longer imported or rendered on this page
- npm run check passes
</success_criteria>

<output>
After completion, create `.planning/phases/27-search-knowledge-base/27-04-SUMMARY.md`
</output>

## TEST-PLAN

Tests to verify this plan's deliverables (added to manifest in Plan 05):
- [P27-17] KB genre page contains genre-type-pill class
- [P27-18] KB genre page contains key-artist-row class
- [P27-19] KB genre page contains genre-map-placeholder class
- [P27-20] KB genre page does NOT import GenreGraph (replaced by placeholder)
- [P27-21] skip — Genre page type badge, compact artist rows, and map placeholder visible in desktop app (requires running desktop app)
