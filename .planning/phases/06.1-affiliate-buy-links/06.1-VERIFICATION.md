---
phase: 06.1-affiliate-buy-links
verified: 2026-02-21T10:30:00Z
status: passed
score: 15/15 must-haves verified
re_verification: false
---

# Phase 06.1: Affiliate Buy Links Verification Report

**Phase Goal:** Add affiliate buy links to release pages so niche-music fans can purchase and support artists, while Mercury earns commission to sustain operations.
**Verified:** 2026-02-21
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (Consolidated from all five plan must_haves)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Affiliate module exists and exports `buildBuyLinks()` function | VERIFIED | `src/lib/affiliates/construct.ts` — 88 lines, full implementation, exported via `index.ts` |
| 2 | Affiliate IDs are read from env vars, never hardcoded | VERIFIED | `config.ts` reads `env.AFFILIATE_AMAZON_TAG`, `env.AFFILIATE_APPLE_TOKEN`, `env.AFFILIATE_APPLE_CAMPAIGN` from `$env/dynamic/private`; grep for `AFFILIATE_AMAZON_TAG` in `construct.ts` returns zero matches |
| 3 | All five buy platforms are always represented in output (Bandcamp, Amazon, Apple Music, Beatport, Discogs) | VERIFIED | `buildBuyLinks()` returns array of exactly 5 via `buildBandcampLink`, `buildAmazonLink`, `buildAppleMusicLink`, `buildBeatportLink`, `buildDiscogsLink` — no platform is conditional |
| 4 | `isDirect` flag correctly distinguishes search fallbacks from direct product links | VERIFIED | Only `buildBandcampLink` sets `isDirect: true` (and only when a direct URL is passed); all other builders hardcode `isDirect: false` |
| 5 | Release detail page exists at `/artist/{slug}/release/{mbid}` returning real data | VERIFIED | `+page.server.ts` fetches MB browse endpoint `release?release-group={mbid}&inc=recordings+artist-credits+media+artist-rels`, returns `ReleaseDetail` with tracklist, credits, buy links |
| 6 | Tracklist is fetched from MusicBrainz using the release-group MBID browse endpoint | VERIFIED | Line 62 of `+page.server.ts`: `release?release-group=${mbid}&inc=recordings+artist-credits+media+artist-rels&limit=1&fmt=json` |
| 7 | Buy links are constructed server-side using affiliate config from env vars | VERIFIED | Lines 149-150 of `+page.server.ts`: `getAffiliateConfig()` then `buildBuyLinks(artistName, rel.title, bandcampUrl, affiliateConfig)` |
| 8 | Tauri desktop context fetches MusicBrainz directly and builds non-coded buy links | VERIFIED | `+page.ts` branches on `isTauri()`, uses dynamic import of `buildBuyLinks` with null affiliate config; `getAffiliateConfig` is NOT imported |
| 9 | MusicBrainz response is cached via Cloudflare Cache API (24hr TTL) | VERIFIED | `platform.caches.default` used; `max-age=86400` header set on cached response |
| 10 | `BuyOnBar` renders all five platform links in a row | VERIFIED | `{#each links as link}` — data-driven, no platform filtering in component |
| 11 | Search fallback links show a `?` indicator | VERIFIED | `{#if !link.isDirect}<span class="search-indicator">?</span>{/if}` in `BuyOnBar.svelte` |
| 12 | All buy links open in a new tab | VERIFIED | `target="_blank" rel="noopener noreferrer"` on every link anchor in `BuyOnBar.svelte` |
| 13 | Clicking a release card navigates to the release detail page | VERIFIED | `ReleaseCard.svelte` has `artistSlug` prop, `releaseHref = /artist/${artistSlug}/release/${release.mbid}`, both cover art and title are wrapped in `<a href={releaseHref}>` |
| 14 | Footer shows affiliate disclosure text | VERIFIED | `+layout.svelte` contains `<footer class="site-footer"><p class="affiliate-disclosure">Some links on release pages are affiliate links...` on all pages |
| 15 | `.dev.vars` has affiliate env var placeholders for local dev | VERIFIED | `.dev.vars` exists with `AFFILIATE_AMAZON_TAG=`, `AFFILIATE_APPLE_TOKEN=`, `AFFILIATE_APPLE_CAMPAIGN=mercury`; `.dev.vars` present in `.gitignore` |

**Score:** 15/15 truths verified

---

### Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `src/lib/affiliates/types.ts` | VERIFIED | 22 lines; exports `BuyPlatform`, `BuyLink` (with `isDirect`), `AffiliateConfig` |
| `src/lib/affiliates/config.ts` | VERIFIED | 24 lines; imports `$env/dynamic/private`; exports `getAffiliateConfig()` |
| `src/lib/affiliates/construct.ts` | VERIFIED | 89 lines; exports `buildBuyLinks()`; 5 private builder functions; no hardcoded affiliate IDs |
| `src/lib/affiliates/index.ts` | VERIFIED | 3 lines; re-exports all public types and functions |
| `src/routes/artist/[slug]/release/[mbid]/+page.server.ts` | VERIFIED | 202 lines; exports `load` as `PageServerLoad`; imports `getAffiliateConfig` and `buildBuyLinks`; MB browse endpoint with Cloudflare caching |
| `src/routes/artist/[slug]/release/[mbid]/+page.ts` | VERIFIED | 130 lines; exports `load` as `PageLoad`; imports `isTauri`; dynamic import of `buildBuyLinks`; does NOT import `getAffiliateConfig` |
| `src/lib/components/BuyOnBar.svelte` | VERIFIED | 131 lines; accepts `links: BuyLink[]` prop; renders `{#each}` loop with `isDirect` check; all links have `target="_blank" rel="noopener noreferrer"` |
| `src/routes/artist/[slug]/release/[mbid]/+page.svelte` | VERIFIED | 342 lines; imports and uses `BuyOnBar`; renders cover art hero, tracklist, credits; handles null release gracefully |
| `src/lib/components/ReleaseCard.svelte` | VERIFIED | Has `artistSlug: string` prop; `releaseHref` derived from `/artist/${artistSlug}/release/${release.mbid}`; cover art and title both wrapped in navigation anchors |
| `src/routes/artist/[slug]/+page.svelte` | VERIFIED | Passes `artistSlug={data.artist.slug}` to each `<ReleaseCard>` |
| `src/routes/+layout.svelte` | VERIFIED | Contains `<footer class="site-footer">` with `.affiliate-disclosure` text — present on all pages |
| `.dev.vars` | VERIFIED | All three affiliate env vars present; file is gitignored |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `config.ts` | `$env/dynamic/private` | SvelteKit dynamic env import | WIRED | Line 1: `import { env } from '$env/dynamic/private'` |
| `construct.ts` | `AffiliateConfig` | `buildBuyLinks()` parameter type | WIRED | Function signature: `config: AffiliateConfig`; uses `config.amazonTag`, `config.appleToken`, `config.appleCampaign` |
| `+page.server.ts` | `src/lib/affiliates/config.ts` | `getAffiliateConfig()` import | WIRED | Line 3: `import { getAffiliateConfig } from '$lib/affiliates/config'`; called on line 149 |
| `+page.server.ts` | `src/lib/affiliates/construct.ts` | `buildBuyLinks()` import | WIRED | Line 4: `import { buildBuyLinks } from '$lib/affiliates/construct'`; called on line 150 |
| `+page.server.ts` | `musicbrainz.org/ws/2/release` | fetch with release-group browse endpoint | WIRED | `release?release-group=${mbid}&inc=recordings+artist-credits+media+artist-rels` |
| `+page.ts` | `isTauri` branch | dynamic import of `buildBuyLinks` | WIRED | `const { buildBuyLinks } = await import('$lib/affiliates/construct')` inside Tauri branch; `getAffiliateConfig` intentionally absent |
| `+page.svelte` (release) | `BuyOnBar.svelte` | import and component usage | WIRED | Line 3: `import BuyOnBar from '$lib/components/BuyOnBar.svelte'`; used as `<BuyOnBar links={release.buyLinks} />` |
| `ReleaseCard.svelte` | release detail page | `<a href=/artist/{slug}/release/{mbid}>` | WIRED | `releaseHref = /artist/${artistSlug}/release/${release.mbid}`; applied to both cover art link and title link |
| `artist/+page.svelte` | `ReleaseCard.svelte` | `artistSlug` prop pass-through | WIRED | `<ReleaseCard {release} artistSlug={data.artist.slug} onplayinline={handlePlayInline} />` |
| `.dev.vars` | `config.ts` | Wrangler reads at dev server startup | WIRED | File contains `AFFILIATE_AMAZON_TAG`, `AFFILIATE_APPLE_TOKEN`, `AFFILIATE_APPLE_CAMPAIGN`; `.dev.vars` in `.gitignore` |

---

### Requirements Coverage

| Requirement | Plans | Description | Status | Evidence |
|-------------|-------|-------------|--------|----------|
| BUY-01 | 02, 04, 05 | Release detail pages exist — cover art, tracklist, personnel credits, and link rows | SATISFIED | `+page.svelte` at `/artist/{slug}/release/{mbid}` renders all four elements; `+page.server.ts` fetches from MusicBrainz |
| BUY-02 | 01, 02, 03, 04, 05 | "Buy on" row showing all five platforms with search fallbacks and subtle search indicator | SATISFIED | `buildBuyLinks()` always returns 5 platforms; `BuyOnBar.svelte` renders `?` indicator via `{#if !link.isDirect}`; used on release page |
| BUY-03 | 01, 02, 05 | Affiliate IDs stored in env vars only — Amazon Associates and Apple Performance Partners supported | SATISFIED | `$env/dynamic/private` used in `config.ts`; no hardcoded IDs in `construct.ts`; `.dev.vars` has placeholder vars |
| BUY-04 | 04, 05 | Site-wide affiliate disclosure in footer — no per-link badges | SATISFIED | `+layout.svelte` footer contains disclosure text on all pages; no per-link disclosure in `BuyOnBar.svelte` |

All four requirements marked as Complete in REQUIREMENTS.md. No orphaned requirements found.

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `+page.svelte` (release) | `.cover-placeholder` CSS class name contains "placeholder" | Info | This is a legitimate cover art fallback UI element (letter placeholder when Cover Art Archive 404s), not an unimplemented feature stub |

No blockers. No TODO/FIXME/HACK comments. No empty implementations. No console.log-only handlers.

---

### Human Verification Required

The following items cannot be verified programmatically:

#### 1. Release page renders correctly end-to-end

**Test:** Start dev server (`npx wrangler pages dev .svelte-kit/cloudflare --port 8788`), navigate to an artist page, click a release card, verify the release page renders at `/artist/{slug}/release/{mbid}`.
**Expected:** Cover art (or placeholder letter), title, year/type badges, artist name link, "Buy on" row with all five platforms each showing `?` indicator, tracklist with M:SS durations.
**Why human:** Requires live MusicBrainz API call and running dev server; cover art URL validity depends on Cover Art Archive availability.

#### 2. Buy links open in new tab and reach correct platform URLs

**Test:** On a rendered release page, click each of the five buy links.
**Expected:** Each opens a new browser tab to the correct platform search results page (Bandcamp search, Amazon search, Apple Music search, Beatport search, Discogs sell listings).
**Why human:** URL correctness and new-tab behavior requires browser interaction.

#### 3. Affiliate tag appended when env vars are set

**Test:** Set `AFFILIATE_AMAZON_TAG=test-20` in `.dev.vars`, restart dev server, click Amazon buy link.
**Expected:** Amazon URL includes `&tag=test-20`.
**Why human:** Requires setting non-empty env var value and inspecting the generated URL.

#### 4. ReleaseCard hover state shows pointer cursor and navigates

**Test:** On an artist page, hover over a release card cover art and title.
**Expected:** Cursor changes to pointer; clicking navigates to the release detail page.
**Why human:** Visual pointer cursor and navigation behavior requires browser interaction.

---

### Gaps Summary

No gaps found. All 15 observable truths verified. All 12 artifacts confirmed substantive and wired. All 10 key links confirmed active. All 4 requirements (BUY-01 through BUY-04) satisfied with implementation evidence. No blocker anti-patterns.

The affiliate buy link system is fully implemented:
- Pure-logic affiliate URL module (`src/lib/affiliates/`) with server-side env var reading
- Release detail page route with MusicBrainz data fetch, Cloudflare 24hr caching, and affiliate link construction
- `BuyOnBar` component rendering all five platforms with search fallback indicator
- Release navigation wired from `ReleaseCard` on artist pages
- Site-wide footer affiliate disclosure
- Local dev env var placeholders in `.dev.vars`, protected from commit via `.gitignore`

---

_Verified: 2026-02-21_
_Verifier: Claude (gsd-verifier)_
