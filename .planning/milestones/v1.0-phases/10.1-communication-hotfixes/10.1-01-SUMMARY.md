---
phase: 10.1-communication-hotfixes
plan: "01"
subsystem: ui
tags: [svelte5, css, nostr, dms, chat]

# Dependency graph
requires:
  - phase: 10-communication-layer
    provides: NIP-17 DM backend (dmState, sendDM, markConversationRead), ChatOverlay, ChatPanel, notifications.svelte.ts chatState
  - phase: 09-community-foundation
    provides: Profile/avatar/shelf components that use --spacing-* CSS variables

provides:
  - CSS compatibility aliases (9 total) making all Phase 9/10 UI elements visible
  - ConversationList.svelte — scrollable DM conversation list with pubkey input for new threads
  - ChatOverlay routing: 'dms' view shows ConversationList; 'dm-thread' shows ChatPanel
  - ChatPanel back navigation: "← Conversations" button in dm-thread view

affects:
  - 10.1-communication-hotfixes (plan 02 depends on this plan's theme.css aliases being present)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Alias-only CSS compatibility layer — existing canonical variables unchanged; Phase 9/10 components get bridge via var(--canonical) aliases
    - Lazy import pattern for ConversationList consistent with RoomDirectory/SessionCreator

key-files:
  created:
    - src/lib/components/chat/ConversationList.svelte
  modified:
    - src/lib/styles/theme.css
    - src/lib/components/chat/ChatOverlay.svelte
    - src/lib/components/chat/ChatPanel.svelte

key-decisions:
  - "CSS aliases are alias-only — 9 new variables aliased to canonical names, no existing variable renamed. Avoids touching 10+ component files."
  - "ConversationList uses lazy {#await import()} pattern matching RoomDirectory/SessionCreator — defers load until first use, avoids circular deps"
  - "npub1 decoded via nip19.decode from nostr-tools (already a dependency), hex validated via /^[0-9a-f]{64}$/i — both paths route directly to dm-thread view"

patterns-established:
  - "CSS alias pattern: add --alias: var(--canonical); to :root when Phase N components use different naming than theme.css"

requirements-completed:
  - COMM-04

# Metrics
duration: 2min
completed: 2026-02-23
---

# Phase 10.1 Plan 01: CSS Aliases + DM Conversation List Summary

**9 CSS compatibility aliases fix invisible Phase 9/10 UI; ConversationList.svelte closes the DM UI gap (GAP-06), giving users a scrollable thread list and npub/hex pubkey input for starting new conversations**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-23T09:29:24Z
- **Completed:** 2026-02-23T09:32:01Z
- **Tasks:** 2
- **Files modified:** 4 (1 created, 3 modified)

## Accomplishments
- Added 9 CSS aliases to theme.css (4 color + 5 spacing) — all Phase 9/10 buttons, borders, and spacing sections now render correctly
- Created ConversationList.svelte (214 lines) with scrollable conversation list, truncated pubkey display, last message preview, unread badge, and new-DM pubkey input
- Rewired ChatOverlay to split 'dms' view (ConversationList) from 'dm-thread' view (ChatPanel) using lazy imports
- Added "← Conversations" back button to ChatPanel, visible only in dm-thread view

## Task Commits

Each task was committed atomically:

1. **Task 1: CSS Variable Aliases (GAP-05)** - `8acc26f` (feat)
2. **Task 2: ConversationList Component + ChatOverlay/ChatPanel Wiring (GAP-06)** - `3726293` (feat)

## Files Created/Modified
- `src/lib/styles/theme.css` — Added 9 CSS compatibility aliases after --progress-bg block
- `src/lib/components/chat/ConversationList.svelte` — New component: scrollable DM list with new-DM pubkey input
- `src/lib/components/chat/ChatOverlay.svelte` — Split 'dms'/'dm-thread' routing; removed static ChatPanel import; both views lazy-loaded
- `src/lib/components/chat/ChatPanel.svelte` — Added back button with dm-thread guard + .back-btn style

## Decisions Made
- **Alias-only CSS approach:** No renaming of canonical variables. Phase 9/10 components already authored against `--spacing-*` and `--bg-primary` naming. Adding aliases is a one-file fix versus touching 10+ component files.
- **Lazy imports consistent with existing pattern:** `{#await import('./ConversationList.svelte')}` matches RoomDirectory and SessionCreator. The original ChatPanel was statically imported in the catch-all; replacing it with lazy imports for both branches is the correct approach.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. The existing 6 `state_referenced_locally` warnings in crate/kb/time-machine pages were pre-existing and unchanged.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness
- GAP-05 and GAP-06 are closed. Phase 10.1 Plan 02 (taste profile publisher + Nostr publish export) can proceed.
- The CSS aliases benefit all Phase 9 community UI immediately without any additional changes.

---
*Phase: 10.1-communication-hotfixes*
*Completed: 2026-02-23*
