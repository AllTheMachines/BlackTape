---
phase: 07-knowledge-base
plan: 07
type: execute
wave: 5
depends_on: [07-06]
files_modified:
  - ARCHITECTURE.md
  - docs/user-manual.md
  - BUILD-LOG.md
autonomous: true
requirements: [KB-01, KB-02, DISC-05, DISC-06, DISC-07]

must_haves:
  truths:
    - "ARCHITECTURE.md documents the Knowledge Base subsystem — GenreGraph, SceneMap, LinerNotes, pipeline Phase G"
    - "ARCHITECTURE.md documents the new genre/genre_relationships DB tables and their purpose"
    - "docs/user-manual.md explains Knowledge Base, Time Machine, and Liner Notes to end users"
    - "BUILD-LOG.md has a Phase 7 entry recording the key decisions and architecture choices"
    - "npm run check passes with 0 errors after all documentation updates"
    - "npm run build completes without errors"
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "Knowledge Base section with subsystem overview, DB schema, and component inventory"
      contains: "Knowledge Base"
    - path: "docs/user-manual.md"
      provides: "User-facing Knowledge Base, Time Machine, and Liner Notes documentation"
      contains: "Knowledge Base"
    - path: "BUILD-LOG.md"
      provides: "Phase 7 build log entry with decisions and architecture notes"
      contains: "Phase 7"
  key_links:
    - from: "ARCHITECTURE.md"
      to: "src/lib/components/GenreGraph.svelte"
      via: "component inventory reference"
      pattern: "GenreGraph"
    - from: "docs/user-manual.md"
      to: "/kb"
      via: "user-facing route documentation"
      pattern: "/kb"
---

<objective>
Complete Phase 7 by updating all documentation — ARCHITECTURE.md (technical reference), docs/user-manual.md (user-facing docs), and BUILD-LOG.md (build documentary) — then run final verification to confirm 0 TypeScript errors and a clean production build.

Purpose: Per CLAUDE.md, docs must be updated whenever features or architecture change. Phase 7 adds a major subsystem (Knowledge Base), three new routes (/kb, /kb/genre/[slug], /time-machine), three new components (GenreGraph, SceneMap, LinerNotes), one new pipeline step, and DB schema extensions. All of this must be documented.
Output: Updated ARCHITECTURE.md, docs/user-manual.md, BUILD-LOG.md, plus verified build.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ARCHITECTURE.md
@docs/user-manual.md
@BUILD-LOG.md
@.planning/phases/07-knowledge-base/07-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ARCHITECTURE.md and docs/user-manual.md</name>
  <files>ARCHITECTURE.md, docs/user-manual.md</files>
  <action>
Read both files fully before editing. Then make targeted additions — do not restructure existing content.

**ARCHITECTURE.md additions:**

1. Add a "Knowledge Base" section to the Feature Subsystems or relevant area. It should document:
   - **Overview**: Genre/scene encyclopedia with four content layers (open data, links/embeds, AI summaries, community-written deferred to Phase 9+)
   - **Routes**: `/kb` (landing, genre graph), `/kb/genre/[slug]` (genre/scene page), `/time-machine` (year browser)
   - **Components**: `GenreGraph.svelte` (D3 force, extended from StyleMap pattern), `SceneMap.svelte` (Leaflet, dynamic import in onMount), `LinerNotes.svelte` (expandable credits, lazy MusicBrainz fetch)
   - **DB tables**: `genres` (slug, name, type: genre|scene|city, wikidata_id, inception_year, origin_lat/lng, mb_tag) and `genre_relationships` (from_id, to_id, rel_type: subgenre|influenced_by|scene_of)
   - **Pipeline**: Phase G (`pipeline/build-genre-data.mjs`) — Wikidata SPARQL fetch + Nominatim geocoding at build time
   - **Query functions**: getGenreSubgraph, getGenreBySlug, getGenreKeyArtists, getArtistsByYear, getStarterGenreGraph — all in `src/lib/db/queries.ts`

2. Add to the Anti-Patterns section (or create it if not present):
   - **Geocoding at runtime**: Nominatim is pipeline-only. Never call at page load (rate limit: 1 req/sec).
   - **Full genre graph at once**: 2000+ genres renders as hairball. Always use subgraph queries (LIMIT 30 neighbors).
   - **Leaflet top-level import**: Causes SSR `window is not defined`. Always `await import('leaflet')` inside onMount.

**docs/user-manual.md additions:**

Add a "Knowledge Base" section with user-facing content:

```markdown
## Knowledge Base

### Genre Map
The Knowledge Base is Mercury's music encyclopedia — a living map of genres, scenes, and musical movements. Find it under **Knowledge Base** in the navigation.

The genre graph starts centered on your taste. Click any genre to open its page. Genres (global), scenes (geographic/temporal), and cities are visually distinct. The graph expands as you navigate — always a neighborhood, never an overwhelming hairball.

### Genre Pages
Each genre or scene page shows:
- A description pulled from Wikipedia when available
- An AI-generated vibe summary (desktop app, when AI is enabled)
- Key artists linked to their Mercury profiles
- Related genres and scenes
- For scenes with a city origin: a map pinning the location

Pages with limited data show a contribution invitation — community editing comes in a future phase.

### Time Machine
The **Time Machine** lets you browse Mercury's catalog by year. Navigate decades (60s–20s) with the decade buttons, then fine-tune within the decade using the year slider. Add a genre tag filter to narrow the results.

Opening state: approximately 30 years in the past — a natural starting point for musical nostalgia. Adjust freely.

### Liner Notes
On any release page, expand **Liner Notes** at the bottom to see:
- Full artist credits
- Record label and catalog number
- Per-track recording credits (when available)

Liner Notes are fetched from MusicBrainz on demand — expanding is instant when MusicBrainz responds quickly. Credits may show a brief loading state.
```

Place it logically among existing feature sections (after Discovery or similar).
  </action>
  <verify>
- `ARCHITECTURE.md` contains "Knowledge Base" (grep confirms)
- `ARCHITECTURE.md` contains "GenreGraph" and "genre_relationships" (grep confirms)
- `docs/user-manual.md` contains "Knowledge Base" section with Time Machine and Liner Notes
- Both files read and edited using the Read tool first (not overwritten blindly)
  </verify>
  <done>
ARCHITECTURE.md documents Knowledge Base subsystem (routes, components, DB schema, pipeline, anti-patterns). docs/user-manual.md explains Knowledge Base, genre pages, Time Machine, and Liner Notes in user-facing language.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update BUILD-LOG.md + run final build verification</name>
  <files>BUILD-LOG.md</files>
  <action>
Read BUILD-LOG.md to understand the current entry format and entry numbering. Then append a Phase 7 entry in the same format. Key content to capture:

**Phase 7 BUILD-LOG.md entry:**
- Entry number: next sequential number after the Phase 06.1 entry
- Date: 2026-02-21
- Title: Phase 7: Knowledge Base
- Key decisions to record:
  - Genre data from Wikidata SPARQL at pipeline time — not manual curation, not runtime
  - Geocoding at pipeline time (Nominatim 1100ms delay) — never at runtime
  - GenreGraph extends StyleMap's headless D3 tick(500) pattern — same anti-pattern applies (never on('tick'))
  - Three node types: genre (circle), scene (diamond), city (small dashed circle)
  - Leaflet loaded via dynamic import in onMount — SSR-safe, no top-level window access
  - Time Machine opening state: current year - 30 (nostalgia default, user's era ±30)
  - LinerNotes lazy-fetches MusicBrainz on expand — never on page load (rate limit consideration)
  - AI genre summary: 2-3 sentences, temperature 0.6, Tauri-only, best-effort (Wikipedia takes priority)
  - genreSummary prompt added to prompts.ts alongside existing artistSummary and recommendation prompts
  - Genre tags on artist pages dual-linked: /discover (existing) + /kb/genre/[slug] (new)
  - Content layers blended seamlessly — no hard tabs; source attribution as small badge/tooltip
  - Community editing mechanics (who can edit, history, moderation) explicitly deferred to Phase 9+

After writing the BUILD-LOG.md entry, run final verification:

```bash
npm run check
npm run build
```

If `npm run check` has errors: fix them before completing this plan. Log any fixes in BUILD-LOG.md.
If `npm run build` has errors: fix them and log.
If both pass: note "0 errors, build clean" in the verification section of the SUMMARY.

Remove any `<!-- status -->` block from BUILD-LOG.md if present (session-end protocol from CLAUDE.md).
  </action>
  <verify>
- `BUILD-LOG.md` contains a Phase 7 entry with date 2026-02-21
- `npm run check` exits with 0 errors
- `npm run build` completes without errors
- No `<!-- status -->` block remains in BUILD-LOG.md
  </verify>
  <done>
BUILD-LOG.md has Phase 7 entry with all key decisions documented. npm run check passes. npm run build passes. Documentation is complete. Phase 7 is done.
  </done>
</task>

</tasks>

<verification>
1. ARCHITECTURE.md contains Knowledge Base section with DB schema, routes, components, pipeline, anti-patterns.
2. docs/user-manual.md contains genre map, time machine, and liner notes documentation.
3. BUILD-LOG.md has Phase 7 entry.
4. `npm run check` — 0 errors.
5. `npm run build` — clean build, no errors.
</verification>

<success_criteria>
- All three doc files updated with Phase 7 content
- ARCHITECTURE.md captures: GenreGraph pattern, Leaflet SSR safety, pipeline Phase G, genre DB tables
- docs/user-manual.md explains KB, genre pages, Time Machine, Liner Notes in plain language
- BUILD-LOG.md records key decisions for Steve's documentary record
- Both npm run check and npm run build pass clean
- Phase 7 fully complete — KB-01, KB-02, DISC-05, DISC-06, DISC-07 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-knowledge-base/07-07-SUMMARY.md`
</output>
