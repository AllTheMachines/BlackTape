---
phase: 06.1-affiliate-buy-links
plan: "05"
type: execute
wave: 4
depends_on:
  - "06.1-04"
files_modified:
  - .dev.vars
autonomous: false
requirements:
  - BUY-01
  - BUY-02
  - BUY-03
  - BUY-04

user_setup:
  - service: amazon-associates
    why: "5% commission on music purchases via Amazon"
    env_vars:
      - name: AFFILIATE_AMAZON_TAG
        source: "Amazon Associates dashboard (affiliate-program.amazon.com) -> Account -> Tracking IDs"
    account_setup:
      - task: "Sign up for Amazon Associates at affiliate-program.amazon.com (free, self-serve, immediate approval)"
  - service: apple-performance-partners
    why: "7% commission on Apple Music purchases"
    env_vars:
      - name: AFFILIATE_APPLE_TOKEN
        source: "Apple Performance Partners (performance-partners.apple.com) -> Partnerize dashboard -> Publisher -> Integration -> at= token"
      - name: AFFILIATE_APPLE_CAMPAIGN
        source: "Optional: any short campaign name you choose (e.g. 'mercury'), max 40 chars"
    account_setup:
      - task: "Apply at performance-partners.apple.com (gated — may take days to weeks; application required; Mercury may need traffic first)"

must_haves:
  truths:
    - "Navigating to /artist/{slug}/release/{mbid} shows a working release page"
    - "Buy on row shows all five platforms"
    - "Search fallback links (Bandcamp, Amazon, Beatport, Discogs, and Apple Music without token) have '?' indicator"
    - "Clicking a platform buy link opens in a new tab"
    - "ReleaseCard on artist page is clickable and navigates to release detail"
    - "Footer affiliate disclosure is visible"
    - ".dev.vars has affiliate env var placeholders for local dev"
  artifacts:
    - path: ".dev.vars"
      provides: "Local affiliate env var placeholders"
      contains: "AFFILIATE_AMAZON_TAG"
  key_links:
    - from: ".dev.vars"
      to: "src/lib/affiliates/config.ts"
      via: "Wrangler reads .dev.vars at dev server startup"
      pattern: "AFFILIATE_"
---

<objective>
Final integration step: set up local dev affiliate env vars, then visually verify the complete phase working end-to-end.

Purpose: Ensure the full user journey works — from artist page to release page, with buy links rendered and clickable. The checkpoint gives Steve a moment to inspect the UI before marking the phase complete.
Output: Working release pages with buy links in both dev and Cloudflare environments.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-affiliate-buy-links/06.1-CONTEXT.md
@.planning/phases/06.1-affiliate-buy-links/06.1-RESEARCH.md
@.planning/phases/06.1-affiliate-buy-links/06.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up .dev.vars with affiliate env var placeholders</name>
  <files>
    .dev.vars
  </files>
  <action>
Check if `.dev.vars` already exists in the project root (alongside `wrangler.jsonc`). If it exists, read its current contents and APPEND the affiliate vars. If it does not exist, create it.

Add the following affiliate configuration block:

```
# Affiliate program configuration
# Set real values to enable commission-coded links.
# Leave as-is or empty for non-coded search fallback links (still functional).
#
# Amazon Associates: sign up at affiliate-program.amazon.com (free, immediate)
AFFILIATE_AMAZON_TAG=
#
# Apple Performance Partners: apply at performance-partners.apple.com (gated, selective)
AFFILIATE_APPLE_TOKEN=
AFFILIATE_APPLE_CAMPAIGN=mercury
```

Important notes:
- `.dev.vars` is Wrangler's secret file for local dev — used by `npx wrangler pages dev`
- `.dev.vars` should already be in `.gitignore` (Wrangler adds it by default). Verify this is the case. If not, add `.dev.vars` to `.gitignore`.
- NEVER commit real affiliate credentials. The placeholders (empty values) are safe to commit.
- `AFFILIATE_APPLE_CAMPAIGN=mercury` is a safe default (no secrets, just a campaign name label).
  </action>
  <verify>
1. File `.dev.vars` exists in project root (same directory as `wrangler.jsonc`).
2. File contains `AFFILIATE_AMAZON_TAG`, `AFFILIATE_APPLE_TOKEN`, `AFFILIATE_APPLE_CAMPAIGN`.
3. Verify `.gitignore` includes `.dev.vars` — run: `grep "dev.vars" .gitignore`.
  </verify>
  <done>
`.dev.vars` has affiliate env var placeholder keys. `.gitignore` contains `.dev.vars`. No real credentials committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — release pages and buy links</name>
  <what-built>
Complete Phase 06.1 implementation:
- Affiliate module (src/lib/affiliates/) with URL construction for all 5 platforms
- Release detail pages at /artist/{slug}/release/{mbid}
- BuyOnBar component with search indicator on fallback links
- Clickable ReleaseCards navigating to release pages
- Footer affiliate disclosure
- .dev.vars with affiliate env var placeholders
  </what-built>
  <how-to-verify>
Start the dev server: `npx wrangler pages dev .svelte-kit/cloudflare --port 8788`
(First run `npm run build` if needed)

**Step 1 — Artist page navigation:**
1. Visit http://localhost:8788
2. Search for an artist (e.g., "Aphex Twin" or "Boards of Canada")
3. Click through to an artist page
4. Confirm the release cards are now clickable — hover should show a pointer cursor on cover art and title

**Step 2 — Release detail page:**
1. Click a release card
2. URL should change to `/artist/{slug}/release/{mbid}`
3. Verify the release page shows:
   - Large cover art (or placeholder letter if Cover Art Archive returns 404)
   - Release title (large heading)
   - Year and type badge
   - Artist name as a clickable link back to the artist page
   - "Buy on" row below the hero with all five platform labels: Bandcamp, Amazon Music, Apple Music, Beatport, Discogs
   - '?' indicator on all links (all are search fallbacks in dev with empty affiliate IDs)
   - Tracklist (numbered tracks with durations if MusicBrainz has them, or "Tracklist not available" message)

**Step 3 — Buy links:**
1. Hover over each buy link — should highlight in platform color
2. Click one (e.g., Discogs) — should open in a NEW TAB
3. Verify the Discogs URL goes to `discogs.com/sell/list?q=...` (marketplace, not database)
4. Click Amazon — should go to `amazon.com/s?k=...` (no affiliate tag since .dev.vars has empty values)
5. Click Bandcamp — should go to `bandcamp.com/search?q=...`

**Step 4 — Back navigation:**
1. Click the artist name link on the release page — should navigate back to the artist page
2. Or use the browser back button

**Step 5 — Footer:**
1. Scroll to the bottom of any page
2. Verify the affiliate disclosure text is visible

**Known acceptable behaviors:**
- Cover art may be a placeholder letter (Cover Art Archive 404s are common)
- Tracklist may show "not available" for releases with sparse MusicBrainz data
- MusicBrainz API may be slow on first load (24hr cache kicks in on subsequent loads)
- All buy links will be non-coded (search fallbacks with '?') since affiliate IDs are not set in .dev.vars
  </how-to-verify>
  <resume-signal>Type "approved" if the release pages and buy links work correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human verification confirms:
- Release pages accessible via ReleaseCard navigation
- Buy on row visible with all 5 platforms
- Search '?' indicator on all links
- Links open in new tab
- Footer disclosure visible
- Build passes
</verification>

<success_criteria>
- All five buy platforms rendered on release pages
- ReleaseCard navigation to release detail pages works
- Footer affiliate disclosure visible
- `.dev.vars` has affiliate placeholder env vars
- No affiliate IDs committed to git
- Human verifier types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-affiliate-buy-links/06.1-05-SUMMARY.md`
</output>
