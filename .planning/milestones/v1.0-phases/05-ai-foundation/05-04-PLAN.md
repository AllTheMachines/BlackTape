---
phase: 05-ai-foundation
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - src/lib/components/AiRecommendations.svelte
  - src/lib/components/FavoriteButton.svelte
  - src/routes/artist/[slug]/+page.svelte
  - src/routes/artist/[slug]/+page.ts
  - src/lib/ai/prompts.ts
autonomous: true

must_haves:
  truths:
    - "Artist pages show AI-powered recommendations when user has enough taste data"
    - "Recommendations section appears alongside (separate from) existing Related Artists discovery panel"
    - "No explanation for recommendations — just artists, per user decision"
    - "Recommendations do NOT appear when taste data is insufficient (no cold-start)"
    - "Favorite/save button on artist pages persists to taste.db"
    - "AI-generated artist summary appears when AI is ready (replaces or supplements Wikipedia bio)"
  artifacts:
    - path: "src/lib/components/AiRecommendations.svelte"
      provides: "Recommendations section for artist pages"
    - path: "src/lib/components/FavoriteButton.svelte"
      provides: "Save/favorite toggle button for artists"
  key_links:
    - from: "src/lib/components/AiRecommendations.svelte"
      to: "src/lib/ai/engine.ts"
      via: "getAiProvider().complete() for recommendations"
      pattern: "getAiProvider.*complete"
    - from: "src/lib/components/AiRecommendations.svelte"
      to: "src/lib/taste/profile.svelte.ts"
      via: "reads tasteProfile.hasEnoughData"
      pattern: "tasteProfile\\.hasEnoughData"
    - from: "src/lib/components/FavoriteButton.svelte"
      to: "src/lib/taste/favorites.ts"
      via: "addFavorite/removeFavorite"
      pattern: "addFavorite|removeFavorite"
---

<objective>
Add AI-powered recommendations and artist summaries to artist pages, plus a favorite/save button. Recommendations use the taste profile + AI to suggest artists. Summaries are AI-generated from public data. Favorites feed the taste profile.

Purpose: Per user decisions, recommendations are "woven into artist pages" as a separate section alongside Related Artists. They only appear when there's enough taste data. The favorite button is the primary explicit signal for taste building.

Output: AiRecommendations component on artist pages, FavoriteButton component, AI-generated artist summaries when bio is unavailable.
</objective>

<execution_context>
@C:\Users\User\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\User\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-ai-foundation/05-RESEARCH.md
@.planning/phases/05-ai-foundation/05-01-SUMMARY.md
@.planning/phases/05-ai-foundation/05-02-SUMMARY.md
@.planning/phases/05-ai-foundation/05-03-SUMMARY.md
@src/routes/artist/[slug]/+page.svelte
@src/lib/components/NowPlayingDiscovery.svelte
@src/lib/ai/prompts.ts
@src/lib/taste/profile.svelte.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: FavoriteButton + AiRecommendations components</name>
  <files>
    src/lib/components/FavoriteButton.svelte
    src/lib/components/AiRecommendations.svelte
    src/lib/ai/prompts.ts
  </files>
  <action>
    **src/lib/components/FavoriteButton.svelte:** Heart/save toggle:
    - Props: `mbid: string`, `name: string`, `slug: string`.
    - On mount (Tauri only): check if artist is favorite via `isFavorite(mbid)`.
    - Click handler: toggle favorite status. Call addFavorite/removeFavorite from taste/favorites.
    - Visual: Small heart icon button. Filled when favorited, outline when not. Use CSS with var(--text-accent) for filled state. Transition on hover.
    - Non-Tauri: don't render (favorites are desktop-only).
    - Uses dynamic import pattern for taste/favorites to isolate Tauri deps.

    **src/lib/components/AiRecommendations.svelte:** AI recommendation section:
    - Props: `artistName: string`, `artistTags: string`, `artistMbid: string`.
    - On mount: Check if AI is ready (`getAiProvider() !== null`) AND taste has enough data (`tasteProfile.hasEnoughData`). If either fails, don't render anything (component is invisible).
    - When ready: Generate recommendations:
      1. Build the taste description from tasteProfile.tags (top 10 tags by weight, formatted as "ambient, electronic, experimental, ...").
      2. Call `getAiProvider().complete()` with `PROMPTS.recommendation(artistName, tasteDescription)`.
      3. Parse response: split by newlines, extract artist names (strip numbering, descriptions).
      4. For each recommended artist name, try to find them in mercury.db via search (using dynamic import to TauriProvider).
      5. Show matched artists as clickable links (link to /artist/[slug]). Unmatched names shown as plain text.
    - Loading state: Show standard skeleton (3 lines, subtle pulse animation — same style as other loading states, per user decision: "standard loading states, AI isn't treated as special").
    - Error/empty state: Show "Not enough data to generate this" per user decision for honest fallback. Or simply don't show the section at all (preference: show nothing rather than an error message).
    - Section styling: Matches existing section-title styling. Section title: "You might also like" (no "AI" branding). List of artist names, each on its own line, clean and simple. No cards, no images — just names. Feels like a curated list.
    - Cache results: Store in a local Map keyed by artistMbid. Don't re-request if already computed this session.

    **src/lib/ai/prompts.ts:** Refine the recommendation prompt:
    - `recommendation`: Make it return ONLY artist names, one per line. System prompt tells the model to focus on artists that exist in music databases (not fictional). Temperature 0.7 for variety.
    - Add `artistSummary` prompt refinement: 2-3 sentences, focus on sound, genre, significance. Temperature 0.5 for factual consistency.
  </action>
  <verify>
    `npm run check` passes. Verify FavoriteButton has toggle logic. Verify AiRecommendations checks both AI readiness and taste threshold.
  </verify>
  <done>
    FavoriteButton toggles artist favorite status with heart icon. AiRecommendations generates personalized suggestions from taste profile, only when enough data exists. Recommendations cached per session. Section invisible when conditions not met.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate recommendations, favorites, and AI summaries into artist page</name>
  <files>
    src/routes/artist/[slug]/+page.svelte
    src/routes/artist/[slug]/+page.ts
  </files>
  <action>
    **src/routes/artist/[slug]/+page.svelte:** Add three new elements:

    1. **FavoriteButton in header:** Add FavoriteButton next to artist name in the header. Pass artist's mbid, name, and slug from data.artist. Only renders in Tauri context.

    2. **AI Summary fallback:** When `data.bio` is empty/null AND AI is ready (isTauri + getAiProvider()):
       - On mount, request an AI-generated summary via `getAiProvider().complete()` with `PROMPTS.artistSummary(name, tags, country)`.
       - Store result in local state.
       - Display in the same `.bio` section as regular bio, with identical styling.
       - Per user decision: no "AI-generated" label.
       - If AI can't produce a result (empty response, error), show nothing (honest fallback: just don't show the bio section).
       - If data.bio exists, don't call AI — Wikipedia bio is preferred.

    3. **AiRecommendations section:** Add below the Discography section (or below Links if no discography). Import and render AiRecommendations component, passing artistName, artistTags, artistMbid from data.

    Ensure existing NowPlayingDiscovery panel (from Phase 4) remains unaffected — it's the tag-based "Related Artists" panel. AiRecommendations is a separate section with its own heading, per user decision: "Sits alongside (enhances) the existing tag-based Related Artists discovery panel, as a separate section."

    **src/routes/artist/[slug]/+page.ts:** No changes needed — the existing universal load function already provides all the data needed (artist info, bio, etc.). AI features are loaded client-side.
  </action>
  <verify>
    `npm run check` passes. Verify artist page imports FavoriteButton and AiRecommendations. Verify AI summary only triggers when bio is null/empty. Verify recommendations section is placed after discography.
  </verify>
  <done>
    Artist pages show: (1) favorite button in header for saving artists, (2) AI-generated bio when Wikipedia bio unavailable, (3) personalized recommendations section when taste data is sufficient. All gated on Tauri + AI ready. Web build unaffected.
  </done>
</task>

</tasks>

<verification>
1. `npm run check` passes
2. FavoriteButton renders on artist pages in Tauri mode
3. AiRecommendations only renders when AI ready + taste threshold met
4. AI summary only generates when data.bio is null/empty
5. Existing Related Artists (NowPlayingDiscovery) panel unaffected
6. Web build shows no AI components (graceful absence)
</verification>

<success_criteria>
Artist pages are enhanced with AI: favorite button lets users save artists (feeding taste profile), recommendations appear when taste data is sufficient, and AI summaries fill in when Wikipedia bio is missing. No cold-start recommendations. No AI labels. Clean integration that feels native.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-foundation/05-04-SUMMARY.md`
</output>
