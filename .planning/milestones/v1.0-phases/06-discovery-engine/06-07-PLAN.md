---
phase: 06-discovery-engine
plan: 07
type: execute
wave: 4
depends_on:
  - "06-03"
  - "06-04"
  - "06-05"
  - "06-06"
files_modified:
  - src/routes/+layout.svelte
  - ARCHITECTURE.md
  - docs/user-manual.md
autonomous: false
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04

must_haves:
  truths:
    - "Discover, Dig, and Style Map nav links appear in the Tauri desktop header"
    - "Discover link also appears in the web header (it works on web)"
    - "ARCHITECTURE.md describes the discovery engine architecture (new routes, queries, pipeline tables)"
    - "docs/user-manual.md explains how to use Discover, Crate Digging, and Style Map"
    - "All four discovery features work end-to-end from the nav bar"
  artifacts:
    - path: "src/routes/+layout.svelte"
      provides: "Nav links for Discover (web+desktop), Dig and Style Map (Tauri-only)"
      contains: "Discover"
    - path: "ARCHITECTURE.md"
      provides: "Discovery Engine section documenting new routes, queries, pipeline tables"
      contains: "Discovery Engine"
    - path: "docs/user-manual.md"
      provides: "User-facing documentation for Discover, Crate Digging, and Style Map features"
      contains: "Crate Digging"
  key_links:
    - from: "src/routes/+layout.svelte"
      to: "src/routes/discover"
      via: "<a href='/discover'>Discover</a>"
      pattern: "href.*discover"
    - from: "src/routes/+layout.svelte"
      to: "src/routes/crate"
      via: "<a href='/crate'>Dig</a> (Tauri-only)"
      pattern: "href.*crate"
---

<objective>
Wire up navigation for all Phase 6 features, then update documentation. Discover appears in both web and Tauri nav (it works on web). Dig and Style Map appear in Tauri-only nav. ARCHITECTURE.md and docs/user-manual.md get Discovery Engine sections.

Purpose: Navigation links make features discoverable. Documentation keeps the project coherent as it grows (CLAUDE.md requires doc updates with every feature addition).
Output: Updated layout.svelte + verification checkpoint + updated docs.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/routes/+layout.svelte
@ARCHITECTURE.md
@docs/user-manual.md
@.planning/phases/06-discovery-engine/06-03-SUMMARY.md
@.planning/phases/06-discovery-engine/06-04-SUMMARY.md
@.planning/phases/06-discovery-engine/06-05-SUMMARY.md
@.planning/phases/06-discovery-engine/06-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navigation links for Discovery Engine features</name>
  <files>src/routes/+layout.svelte</files>
  <action>
Read the current +layout.svelte. It has a Tauri-only nav block with Library, Explore, Settings links. Add the new discovery routes:

1. **Discover** — Works on web AND Tauri. Add it to the web header (outside the `{#if tauriMode}` block) AND inside the Tauri nav block. On web, it's the only discovery link. On Tauri, it joins the existing nav links.

2. **Dig** (Crate Digging) — Tauri-only. Add inside the `{#if tauriMode}` nav block alongside Library, Explore, Settings.

3. **Style Map** — Works on web AND Tauri (data loaded server-side on web). Add similarly to Discover — available on both.

The current nav order (Tauri): Library | Explore | Settings
New nav order (Tauri): Discover | Style Map | Dig | Library | Explore | Settings

On web (non-Tauri), add a slim nav with just: Discover | Style Map

Study the existing nav-link CSS class — new links should use the same class.

Implementation approach: Read the existing layout carefully. Determine the minimal change to add the links without breaking existing structure. The web nav should be subtle — two links only, same style as Tauri nav-link class.
  </action>
  <verify>Run `npm run check`. Start `npm run dev` and visit the landing page — Discover and Style Map links should be visible in the header. In Tauri dev mode, all nav links should be visible including Dig.</verify>
  <done>Navigation links added. Discover and Style Map visible on web. All five links (Discover, Style Map, Dig, Library, Explore, Settings) visible in Tauri header. npm run check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update ARCHITECTURE.md and docs/user-manual.md</name>
  <files>
    ARCHITECTURE.md
    docs/user-manual.md
  </files>
  <action>
Read both files in full before modifying them.

**ARCHITECTURE.md additions:**

Add a new "## Discovery Engine" section after the existing "## Discovery Bridge" section. Include:

- New routes: /discover, /crate, /style-map — which are web-only vs Tauri-only vs both
- New pipeline tables: tag_stats (per-tag popularity) and tag_cooccurrence (co-occurrence edges between tag pairs)
- New query functions in queries.ts: getPopularTags, getArtistsByTagIntersection, getDiscoveryRankedArtists, getCrateDigArtists, getArtistUniquenessScore, getStyleMapData
- Composite discovery score formula: tag rarity (1/artist_count per tag) × tag count inverse × recency × active status
- TagFilter component: URL-driven tag intersection via goto() and page.url.searchParams
- StyleMap component: D3 force simulation — static layout via simulation.tick(500), not reactive on tick
- UniquenessScore component: Badge on artist pages, score to tier mapping
- Anti-patterns documented: ORDER BY RANDOM() on 2.8M rows, on-demand co-occurrence JOIN, D3 DOM manipulation in Svelte

Update the Directory Structure section to include the new routes and components.

Update the Data Model section to document tag_stats and tag_cooccurrence tables in the mercury.db section.

**docs/user-manual.md additions:**

Add a "Discovery Engine" section explaining:

1. **Discover** (/discover): Start from popular genres, click to intersect tags. Niche-first ordering — artists with rarer, more specific tags appear first. Max 5 tags at once. URL is shareable.

2. **Uniqueness Score**: Badge on artist profiles (Very Niche / Niche / Eclectic / Mainstream). Explains what it means and why niche artists score higher.

3. **Crate Digging** (/crate) [Desktop only]: Serendipitous random discovery. Pick genre/decade/country, click Dig. Each click is a surprise.

4. **Style Map** (/style-map): Visual genre relationship map. Node size = how many artists. Edge weight = how often genres appear together. Click a node to browse that genre.

Keep the writing style consistent with existing docs/user-manual.md content.
  </action>
  <verify>Read both updated files to confirm the sections were added correctly and don't break existing content. Run `npm run check` to confirm no TypeScript issues from ARCHITECTURE.md changes (it's a .md file — no type issues, but check for broken imports in layout.svelte).</verify>
  <done>ARCHITECTURE.md has Discovery Engine section with route list, pipeline tables, query functions, and anti-patterns. docs/user-manual.md has user-facing explanations for all four discovery features. No existing content broken.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 6: Discovery Engine — all four features:
    1. /discover — Tag intersection browser with niche-first ranking
    2. Uniqueness score badge on all artist pages
    3. /crate — Crate Digging Mode (Tauri-only random sampling)
    4. /style-map — D3 force-directed genre relationship visualization
    Plus: navigation links, pipeline tables, updated docs
  </what-built>
  <how-to-verify>
    **1. Discover page (web or Tauri)**
    - Visit /discover — tag cloud of genres should appear
    - Click "shoegaze" — URL updates to ?tags=shoegaze, results filter
    - Click "post-rock" — URL updates to ?tags=shoegaze,post-rock, results intersect
    - Artists shown should have BOTH tags
    - Remove a tag — results update
    - With no tags: "niche" artists appear first (verify a very niche band appears above a mainstream one)

    **2. Uniqueness score badge (web or Tauri)**
    - Visit an artist page for a well-known mainstream artist
    - Visit an artist page for an obscure niche artist
    - Verify badge label differs (e.g., "Eclectic" vs "Very Niche")
    - Artists with no tags should have no badge

    **3. Crate Digging (Tauri only)**
    - Visit /crate in the desktop app
    - Click "Dig" — 20 random artists appear
    - Click "Dig" again — different artists appear
    - Type "shoegaze" in genre field, click "Dig" — only shoegaze artists should appear
    - Select "1990s" decade — only artists with begin_year 1990-1999

    **4. Style Map (web or Tauri)**
    - Visit /style-map — SVG with tag nodes should render
    - Node sizes should vary (rock/pop = large, niche genres = small)
    - Hovering a node highlights it
    - Clicking "shoegaze" navigates to /discover?tags=shoegaze

    **5. Navigation**
    - Web: Discover and Style Map links in header
    - Tauri: Discover, Style Map, Dig, Library, Explore, Settings in header
  </how-to-verify>
  <resume-signal>Type "approved" if all features work, or describe which features have issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run check` passes with no errors
2. All four discovery routes load without 500 errors
3. Tag intersection correctly AND-filters artists
4. Uniqueness badge appears on artist pages and varies by artist
5. Crate digging returns different results each click
6. Style map SVG renders with nodes and edges
7. Nav links present in correct contexts (web vs Tauri)
8. ARCHITECTURE.md and docs/user-manual.md have discovery engine content
</verification>

<success_criteria>
All four Phase 6 requirements satisfied:
- DISC-01: Tag intersection browsing at /discover
- DISC-02: Discovery ranking (niche-first) + uniqueness badge on artist pages
- DISC-03: Style map visualization at /style-map
- DISC-04: Crate Digging Mode at /crate

Human verification passed. Documentation updated. Navigation wired.
</success_criteria>

<output>
After completion, create `.planning/phases/06-discovery-engine/06-07-SUMMARY.md`
</output>
