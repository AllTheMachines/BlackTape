---
phase: 02-search-and-embeds
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/styles/theme.css
  - src/app.html
  - src/routes/+layout.svelte
  - src/routes/+page.svelte
  - src/lib/components/SearchBar.svelte
  - src/lib/components/TagChip.svelte
autonomous: true

must_haves:
  truths:
    - "Landing page has a big search bar front and center (search-first, Google-style)"
    - "Dark + minimal aesthetic applied globally (dark background, clean typography)"
    - "Search bar navigates to /search?q=...&mode=artist or /search?q=...&mode=tag"
    - "Tag chips are reusable and clickable, navigating to tag search"
  artifacts:
    - path: "src/lib/styles/theme.css"
      provides: "CSS custom properties for dark theme"
      contains: "--bg-base"
    - path: "src/routes/+layout.svelte"
      provides: "Global layout with dark theme import"
      contains: "theme.css"
    - path: "src/routes/+page.svelte"
      provides: "Search-first landing page"
      contains: "SearchBar"
    - path: "src/lib/components/SearchBar.svelte"
      provides: "Reusable search input with mode toggle"
      contains: "mode"
    - path: "src/lib/components/TagChip.svelte"
      provides: "Clickable tag chip component"
      contains: "/search"
  key_links:
    - from: "src/routes/+page.svelte"
      to: "src/lib/components/SearchBar.svelte"
      via: "component import"
      pattern: "import.*SearchBar"
    - from: "src/lib/components/SearchBar.svelte"
      to: "/search"
      via: "form action or goto() on submit"
      pattern: "/search"
    - from: "src/lib/components/TagChip.svelte"
      to: "/search?mode=tag"
      via: "anchor href"
      pattern: "mode=tag"
---

<objective>
Build the visual foundation and landing page for Mercury.

Purpose: Establishes the dark, information-dense aesthetic that defines Mercury's identity. The landing page is the first thing users see — a big search bar that says "start searching." The SearchBar and TagChip components are reused across search results and artist pages.

Output: Global dark theme, functional landing page with search bar, reusable SearchBar and TagChip components.
</objective>

<execution_context>
@C:\Users\User\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\User\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-search-and-embeds/02-CONTEXT.md
@.planning/phases/02-search-and-embeds/02-RESEARCH.md
@src/lib/config.ts
@src/routes/+layout.svelte
@src/routes/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dark theme and global layout</name>
  <files>src/lib/styles/theme.css, src/app.html, src/routes/+layout.svelte</files>
  <action>
    1. Create `src/lib/styles/theme.css` with CSS custom properties:
       - Background layers: --bg-base (#0a0a0a), --bg-surface (#111111), --bg-elevated (#1a1a1a), --bg-hover (#222222)
       - Text hierarchy: --text-primary (#e0e0e0), --text-secondary (#888888), --text-muted (#555555), --text-accent (#ffffff)
       - Borders: --border-subtle (#1e1e1e), --border-default (#2a2a2a), --border-hover (#3a3a3a)
       - Interactive: --link-color (#7eb8da), --tag-bg (#1a2332), --tag-text (#7eb8da), --tag-border (#2a3a4a)
       - Platform colors: --bandcamp-color (#1da0c3), --spotify-color (#1db954), --soundcloud-color (#ff5500), --youtube-color (#ff0000)
       - Typography: --font-sans (system stack), --font-mono (Cascadia Code, JetBrains Mono, Fira Code)
       - Spacing scale: --space-xs (0.25rem) through --space-2xl (3rem)
       - Layout: --max-width (1200px), --card-radius (6px), --input-radius (6px)
       - Include base element resets: body background, color, font-family, margin 0, box-sizing border-box
       - Include base anchor styles using --link-color
       - Scrollbar styling for dark theme (webkit + Firefox)

    2. Update `src/app.html`:
       - Add `class="dark"` to `<html>` tag (for potential future light mode toggle)
       - Keep existing structure

    3. Rewrite `src/routes/+layout.svelte`:
       - Import `$lib/styles/theme.css` (global styles)
       - Import `PROJECT_NAME` from `$lib/config`
       - Import favicon from `$lib/assets/favicon.svg`
       - Layout structure:
         - `<header>` with project name (linked to `/`) on left, minimal nav (just the name for now)
         - `<main>` slot for page content
         - No footer (information-dense = no wasted space)
       - Header: sticky top, --bg-surface background, subtle bottom border (--border-subtle), compact height
       - The header should be minimal — project name only, no hamburger menus or nav links. This is a search engine, not a portal.
       - Style the header with scoped CSS using theme variables
       - Keep `{@render children()}` pattern for Svelte 5
  </action>
  <verify>Run `npm run dev` — page loads with dark background, system font, themed header. Run `npm run check` — no errors.</verify>
  <done>Dark theme applied globally via CSS custom properties. Layout has sticky header with project name. All pages inherit dark aesthetic.</done>
</task>

<task type="auto">
  <name>Task 2: Build landing page with functional search bar and reusable components</name>
  <files>src/routes/+page.svelte, src/lib/components/SearchBar.svelte, src/lib/components/TagChip.svelte</files>
  <action>
    1. Create `src/lib/components/SearchBar.svelte`:
       - Props: `initialQuery?: string`, `initialMode?: 'artist' | 'tag'`, `size?: 'large' | 'normal'` (large for landing, normal for search page)
       - State: `query` (bound to input), `mode` (artist or tag)
       - Mode toggle: Two buttons/tabs above the search input — "Artists" and "Tags". Active mode highlighted. Use simple styled buttons, not a complex tab component.
       - Input: `type="search"`, placeholder changes based on mode ("Search artists..." vs "Search by tag...")
       - On submit (Enter key or form submit): Navigate to `/search?q={query}&mode={mode}` using SvelteKit's `goto()` from `$app/navigation`
       - Large variant: bigger font, more padding (for landing page hero)
       - Normal variant: compact (for search results page header)
       - Style with theme variables. Input: --bg-elevated background, --border-default border, --text-primary color. Focus state: --border-hover border.
       - Debounce is NOT needed here — search triggers on submit, not on keystroke. Instant results come from the search page itself later.

    2. Create `src/lib/components/TagChip.svelte`:
       - Props: `tag: string`, `count?: number`, `clickable?: boolean` (default true)
       - Renders as an `<a>` tag linking to `/search?q={tag}&mode=tag` when clickable
       - Renders as a `<span>` when not clickable
       - Style: --tag-bg background, --tag-text color, --tag-border border, small rounded pill shape
       - Optional count shown as subdued number after tag name
       - Hover state: slightly lighter background

    3. Rewrite `src/routes/+page.svelte`:
       - Import `SearchBar` and `PROJECT_NAME`, `PROJECT_TAGLINE` from config
       - Hero section centered vertically and horizontally (like current page but functional):
         - Project name (h1, large, light weight, letter-spaced, uppercase)
         - Tagline (subtitle, muted color)
         - SearchBar component in `size="large"` mode
       - Remove the "Coming soon" disabled state — search is now functional
       - Remove the old `<style>` block with body styles (those are now in theme.css)
       - Keep it clean and minimal. No extra content below the search bar. The whole point is to start searching.
       - Style the hero centering with scoped CSS (flexbox centering, min-height: calc(100vh - header height))
  </action>
  <verify>Run `npm run dev` — landing page shows centered hero with project name, tagline, and functional search bar. Mode toggle switches between "Artists" and "Tags". Typing a query and pressing Enter navigates to `/search?q=...&mode=...`. TagChip renders as a styled pill. Run `npm run check` — no errors.</verify>
  <done>Landing page is search-first with big search bar. SearchBar component supports mode toggle and navigates to search route. TagChip component is reusable across the app. All styled with dark theme.</done>
</task>

</tasks>

<verification>
1. `npm run dev` shows dark-themed landing page with functional search
2. `npm run check` passes with zero errors
3. Search bar mode toggle switches between Artists and Tags
4. Submitting search navigates to `/search?q=...&mode=...`
5. TagChip renders as clickable pill linking to tag search
6. Header shows project name linked to home
7. Responsive on mobile (search bar doesn't overflow)
</verification>

<success_criteria>
- Dark, information-dense aesthetic established and applied globally
- Landing page is search-first: centered search bar, no clutter
- SearchBar and TagChip are reusable components ready for search results and artist pages
- Mode toggle provides distinct artist name vs tag search entry points
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-and-embeds/02-02-SUMMARY.md`
</output>
