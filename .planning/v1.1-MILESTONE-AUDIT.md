---
milestone: v1.1
phase_scope: Phase 11 (Scene Building) — first completed phase of v1.1
audited: 2026-02-23T15:00:00Z
status: tech_debt
scores:
  requirements: 2/2
  phases: 4/4
  integration: 4/5
  flows: 4/5
gaps:
  requirements: []
  integration:
    - id: "WEB-SCENES-UNCONNECTED"
      description: "/api/scenes GET endpoint exists and queries tag_cooccurrence correctly, but +page.server.ts for /scenes returns hardcoded { scenes: [] } instead of fetching from it. Web users always see the empty state."
      affected_requirements: ["COMM-07", "COMM-08"]
      severity: deferred
      resolution: "+page.server.ts should fetch('/api/scenes') and return result as scenes; +page.svelte should render data.scenes on web path (guarded by !isTauri())"
  flows:
    - id: "WEB-SCENE-DISCOVERY"
      description: "Web /scenes page shows only empty state — proto-scenes from /api/scenes are never fetched or rendered. Tauri path fully works."
      affected_requirements: ["COMM-07", "COMM-08"]
      severity: deferred
      documented_in: "src/routes/scenes/+page.server.ts comment: 'Future: compute scenes from Nostr public data or D1 snapshot'"
tech_debt:
  - phase: "11-scene-building"
    items:
      - "/api/scenes route exists but has no consumer — +page.server.ts hardcodes { scenes: [] }. Web discovery is fully deferred."
      - "data.scenes received in +page.svelte but never referenced in template — dead prop until web path is wired"
      - "SceneSuggestion type (types.ts line 28) exported but slug page uses inline-typed invoke — minor type consistency gap, not a runtime issue"
      - "Test suite (tools/test-suite/manifest.mjs) has no Phase 11 entries — stale SceneMap.svelte reference at line 424 will produce false failure"
      - "Niche threshold divergence: detection.ts uses artist_count < 5000, /api/scenes uses artist_count < 200 — intentional but undocumented in the route file"
      - "Web /scenes empty state message ('Add artists to your library and favorites') is misleading for web users who have no library"
---

# Milestone Audit: v1.1 Phase 11 — Scene Building

**Scope:** Phase 11 is the first completed phase of v1.1. Phases 12–15 are planned but not yet built. This is a partial milestone audit for the completed phase only.

**Audited:** 2026-02-23
**Status:** tech_debt — all requirements satisfied, no critical blockers, accumulated deferred items

---

## Requirements Coverage

| REQ-ID | Description | Phase | Plans | VERIFICATION.md | SUMMARY Frontmatter | ROADMAP Traceability | → Final Status |
|--------|-------------|-------|-------|-----------------|---------------------|----------------------|----------------|
| COMM-07 | AI scene awareness — detects emerging scenes from collective listening patterns | 11 | 11-01, 11-02, 11-03, 11-04 | passed (line 102) | listed (11-01, 11-04) | [x] in v1.0-ROADMAP.md | **satisfied** |
| COMM-08 | The underground is alive — scenes exist in Mercury that exist nowhere else | 11 | 11-01, 11-02, 11-03, 11-04 | passed (line 103) | listed (11-01, 11-04) | [x] in v1.0-ROADMAP.md | **satisfied** |

**Score: 2/2 requirements satisfied**

No orphaned requirements. COMM-07 and COMM-08 are the only REQ-IDs mapped to Phase 11 across all four plans, RESEARCH.md, and ROADMAP.md.

**3-Source Verification:**

| REQ-ID | VERIFICATION.md | SUMMARY Frontmatter | ROADMAP `[x]` | Final |
|--------|----------------|---------------------|---------------|-------|
| COMM-07 | SATISFIED — detectScenes() pipeline with tag clustering + listener overlap + genres novelty check | 11-01-SUMMARY, 11-04-SUMMARY | ✓ | **satisfied** |
| COMM-08 | SATISFIED — niche filter <5000 artists, isNovelTagCombination(), Fisher-Yates shuffle, emerging tier | 11-01-SUMMARY, 11-04-SUMMARY | ✓ | **satisfied** |

---

## Phase Verification Summary

| Phase | Plans | VERIFICATION.md | Status | Critical Gaps | Tech Debt |
|-------|-------|-----------------|--------|---------------|-----------|
| 11-scene-building | 4/4 complete | present, status: passed | ✓ passed | 0 | 1 (CSS border-opacity warning) |

**Score: 4/4 plans complete, VERIFICATION.md present and passing**

---

## Integration Check Results

### Connected Wiring (all verified)

| From | To | Via | Status |
|------|----|-----|--------|
| `scenes.svelte.ts` | `ndkState` (Phase 10) | Dynamic import `$lib/comms/nostr.svelte.js` | CONNECTED — NIP-51 kind 30001 fetch-before-publish |
| `[slug]/+page.svelte` | `PROMPTS.sceneDescription` (Phase 5) | `getAiProvider()` + `PROMPTS.sceneDescription()` in onMount | CONNECTED — AI description slot correctly scoped to slug page |
| `detection.ts` | `DbProvider` → mercury.db (Phase 6) | `getProvider()` → `tag_cooccurrence` + `tag_stats` queries | CONNECTED — niche filter calibrated for 2.8M MusicBrainz dataset |
| `detection.ts` | `isNovelTagCombination()` → `genres` table (Phase 7) | `db.get()` with try/catch fallback | CONNECTED WITH SAFE FALLBACK — returns `true` if genres table missing |
| `scenes.svelte.ts` | `follow_scene` Tauri command → taste.db | `invoke('follow_scene')` | CONNECTED — all 8 scene commands registered in lib.rs |
| `/scenes` nav link | `+layout.svelte` web + Tauri nav blocks | `href="/scenes"` + `class:active` | CONNECTED — both nav blocks have the link |
| All isTauri() guards | Web build isolation | Dynamic imports + `if (!isTauri()) return` | CONNECTED — zero Tauri API leakage to web bundle |

### Integration Gap (deferred)

| From | To | Issue | Severity |
|------|----|-------|----------|
| `/scenes/+page.server.ts` | `/api/scenes` | Server load hardcodes `{ scenes: [] }` instead of fetching from the API route | deferred — documented as future in source comment |

---

## E2E Flow Verification

| Flow | Status | Notes |
|------|--------|-------|
| Tauri Scene Discovery | **COMPLETE** | favorites → validateListenerOverlap → detectScenes → partitionScenes → SceneCard grid |
| Scene Detail + AI Description | **COMPLETE** | slug lookup → artists/tracks from mercury.db → AI description async in onMount |
| Follow Scene + NIP-51 | **COMPLETE** | followScene() → taste.db + optional NDK NIP-51 kind 30001 |
| Artist Suggestion | **COMPLETE** | suggest form → suggestArtist() → scene_suggestions table |
| Feature Request Vote | **COMPLETE** | vote CTA → upvoteFeatureRequest() → taste.db (Tauri) / localStorage (web) |
| Web Scene Discovery | **BROKEN (deferred)** | /api/scenes exists but server load doesn't call it; web shows empty state only |

**Score: 4/5 flows complete — 1 broken (deferred, documented)**

**Live UAT validation:** The 3 human-verification items from VERIFICATION.md were tested live during this session. 6 bugs were found and fixed:
- niche filter threshold raised 200 → 5000 for 2.8M MusicBrainz dataset
- BLOCKED_TAGS set added for garbage MusicBrainz tags (nazi, rac, racist, twats)
- MAX_CLUSTER_TAGS = 8 cap added to prevent mega-cluster collapse
- Detection moved to onMount (isTauri() unreliable during SSR)
- isNovelTagCombination() wrapped in try/catch for missing genres table
- listenerCount === 0 filter added (scenes must match user taste)

All 3 human-verification items PASSED after fixes.

---

## Tech Debt Summary

| Item | Phase | Severity | Track In |
|------|-------|----------|---------|
| `/api/scenes` not connected to /scenes server load | 11 | low — web scenes intentionally deferred | Phase 12+ or dedicated gap phase |
| `data.scenes` dead prop in +page.svelte | 11 | low — follows from above | Phase 12+ |
| `SceneSuggestion` type unused (inline type used instead) | 11 | info — no runtime impact | Next Phase 11 edit |
| Test suite missing Phase 11 entries + stale `SceneMap.svelte` ref | 11 | low — false failure risk | Address in Phase 12 or standalone cleanup |
| Niche threshold divergence (5000 in detection, 200 in /api/scenes) | 11 | info — intentional, undocumented | Add comment to /api/scenes/+server.ts |
| Web empty state message misleading for web users | 11 | info — cosmetic | Fix with Phase 12 web scenes |

**Total: 6 items across 1 phase — no critical blockers**

---

## Milestone Scope Note

This audit covers Phase 11 only (the first completed phase of v1.1). Phases 12–15 and Phase 0 are planned but not yet built. The full v1.1 milestone audit will be conducted after all phases are complete.

The two requirements assigned to Phase 11 (COMM-07, COMM-08) are satisfied with concrete implementation evidence. The web path for scenes is a documented deferred feature — the Tauri desktop implementation is complete and tested live.

---

_Audited: 2026-02-23_
_Auditor: Claude (gsd-audit-milestone orchestrator + gsd-integration-checker)_
