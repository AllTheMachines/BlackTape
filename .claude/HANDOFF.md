# Work Handoff - 2026-03-04

## Current Task
Set up a Hetzner Cloud server (`blacktape-db`) with PostgreSQL + MusicBrainz data as the backend for BlackTape — replacing the distributed SQLite model so users only download the app, not a database file.

## Context
The current architecture requires users to download a separate ~3GB SQLite database file. Moving to a server-based model means the app connects to a hosted Postgres DB — users just install the app and it works immediately. The server costs ~€7.49/month (CAX21). A Hetzner Cloud server was just created via API and is ready for setup.

## Progress

### Completed
- All UAT gap fixes from phases 35 + 36 committed (artist slugs, World Map query, tag empty state, bio fallback, etc.)
- Strategic decision made: move to server-hosted Postgres instead of distributed SQLite
- Hetzner Cloud API token generated by Steve
- **Server created via API:**
  - Name: `blacktape-db`
  - IP: `46.225.239.209`
  - Type: CAX21 (ARM64, 4 vCPU, 8GB RAM, 80GB SSD)
  - Location: Nuremberg (nbg1) — Falkenstein was in outage
  - OS: Ubuntu 24.04
  - SSH key: `controlcenter-vps` (ID 107939785)

### In Progress
- Server was just created (status: initializing) — SSH not yet attempted
- Nothing installed yet

### Remaining
1. Wait for server to boot, then SSH in
2. Install PostgreSQL 16
3. Tune Postgres (shared_buffers, work_mem for 8GB RAM)
4. Create `blacktape` database + user
5. Download MusicBrainz dump (mbdump-derived.tar.bz2 — ~3GB)
6. Import into Postgres using the pipeline scripts
7. Set up a lightweight REST API (Hono or Fastify) on port 3000
8. Configure UFW firewall (allow 22, 80, 443, 3000 from Tailscale only or public)
9. Set up PM2 for API process management
10. Point the BlackTape app at the new API (update db/provider)
11. Add Tailscale for secure access (same pattern as ControlCenter)

## Key Decisions
- **Nuremberg (nbg1)** instead of Falkenstein — FSN1 had an active outage
- **CAX21** (ARM64, €7.49/mo) — right size for 4-8GB focused dataset; upgrade path to CX42 if needed
- Desktop app will query the server API — local SQLite becomes optional cache only
- Users never download a DB file — app ships ~80MB, first search works immediately
- Donations model: ask when infra costs exceed €50/month
- MusicBrainz daily replication packets to stay current (not monthly full rebuild)

## Sensitive Info (NOT in git — stored in memory only)
- Hetzner API token is in memory file: `C:/Users/User/.claude/projects/D--Projects-BlackTape/memory/hetzner.md`
- SSH private key: `~/.ssh/controlcenter_vps`
- SSH into server: `ssh -i ~/.ssh/controlcenter_vps root@46.225.239.209`

## Relevant Files
- `pipeline/build-geocoding.mjs` — geocoding pipeline (already ran)
- `pipeline/import.js` — MusicBrainz import (needs to run against server Postgres)
- `src/lib/db/provider.ts` — DB provider abstraction (needs updating to point to server)
- `src/lib/db/queries.ts` — all query functions (already fixed for this session)
- `~/.ssh/controlcenter_vps` — SSH private key for Hetzner servers

## Git Status
Only BUILD-LOG.md has uncommitted changes (status block). All code fixes committed.

## Next Steps
1. SSH into server: `ssh -i ~/.ssh/controlcenter_vps root@46.225.239.209`
2. Run server setup: install Postgres 16, create DB, tune config
3. Download + import MusicBrainz data (runs overnight)
4. Set up API server
5. Update app to point at server

## Resume Command
After running `/clear`, run `/resume` to continue.
