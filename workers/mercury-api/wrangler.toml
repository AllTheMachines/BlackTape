name = "mercury-api"
main = "src/index.ts"
compatibility_date = "2025-03-01"

# Custom domain: api.blacktape.org (configure in Cloudflare dashboard)
# routes = [{ pattern = "api.blacktape.org", custom_domain = true }]

[[d1_databases]]
binding = "DB"
database_name = "mercury-db"
database_id = "PLACEHOLDER"  # Run: wrangler d1 create mercury-db → paste ID here
